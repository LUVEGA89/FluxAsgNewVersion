@model Reporting.Service.Web.UI.Models.UserModel
@using Microsoft.AspNet.Identity
@using Reporting.Service.Core.Common
@{
    ViewBag.Title = "Notas de Credito";
}

<style>
    .widget-user .box-footer {
        padding-top: 100px !Important;
    }

    .carousel-inner > .item > a > img, .carousel-inner > .item > img, .img-responsive, .thumbnail a > img, .thumbnail > img {
        display: block;
        width: 100%;
        height: 250px;
    }

    .btn-spacing {
        margin-right: 10px;
    }

    #estatus-rechazado {
        text-align: center;
        color: red;
    }

    #select-concepto-descuento option[value="37"] {
        background: yellow;
    }

    #select-concepto-descuento option[value="39"] {
        background: yellow;
    }

    .btn-spacing {
        margin-right: 10px;
    }

    .widget-user .widget-user-header {
        padding: 10px;
        height: 280px;
        border-top-right-radius: 3px;
        border-top-left-radius: 3px;
    }

    .carousel-caption {
        position: absolute;
        right: 0;
        padding-top: 20px;
        padding-bottom: 20px;
        color: #fff;
        text-align: center;
        bottom: 0;
        left: 0;
        z-index: 10;
        background: rgba(0,0,0,0.4);
    }

    .meta-date-gestion {
        background: #F2635F !important
    }
</style>
<section class="content-header">
    <h1>
        Nota de Crédito Solicitud
        <small id="span-cliente"></small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Nota de Crédito</a></li>
        <li class="active">Solicitud</li>
    </ol>
</section>
<section class="content">
    <div class="row">
        <div class="col-md-4" id="cabecera">
            <div class="box box-danger">
                <div class="box-header">
                    <i class="fa fa-search"></i>
                    <h3 class="box-title">Motór de busqueda</h3>
                </div>
                <!-- FORMULARIO CABECERA -->
                <form role="form">
                    <div class="box-body">

                        <div class="form-group">
                            <label for="">Cliente</label>
                            <div class="input-group">
                                <div class="input-group-addon">
                                    <span class="glyphicon glyphicon-search"></span>
                                </div>
                                <input type="text" class="form-control" id="input-cliente" placeholder="Nombre / Codigo" data-behavior="autocomplete" value="" required>
                            </div>
                        </div>

                        <div class="form-group" id="div-custom-origen">
                            <label for="">Factura origen</label>
                            <input class="form-control" id="input-factura-origen" type="text" value="" required>
                            <span class="help-block span-help-block-origen"></span>
                        </div>

                        <div class="form-group" id="div-custom-destino">
                            <label>Factura destino</label>
                            <input class="form-control" id="input-factura-destino" type="text" value="" required>
                            <!-- INVENTARIO -->
                            <input id="input-factura-destino-monto-permitido" type="hidden">
                            <input id="input-factura-destino-monto-solicitado" type="hidden">
                            <input id="input-factura-destino-monto-disponible" type="hidden">
                            <!-- SOLICITUD SERVICIO -->
                            <input id="input-factura-destino-monto-permitido-servicio-descuento" type="hidden">
                            <input id="input-factura-destino-monto-solicitado-servicio-descuento" type="hidden">
                            <input id="input-factura-destino-monto-disponible-servicio-descuento" type="hidden">
                            <input id="input-factura-destino-cuenta-servicio-descuento-hidden" type="hidden">
                            <!-- SOLICITUD OTRO SERVICIO -->
                            <input type="text" style="display:none;" id="input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden">
                            <input type="text" style="display:none;" id="input-factura-destino-monto-permitido-servicio-otro-descuento-hidden">
                            <input type="hidden" id="input-factura-destino-monto-solicitado-servicio-otro-descuento-hidden">
                            <input type="hidden" id="input-factura-destino-cuenta-servicio-otro-descuento-hidden">



                            <span class="help-block span-help-block-monto-destino"></span>
                            <span class="help-block span-help-block-destino-monto-ocupado"></span>
                            <span class="help-block span-help-block-destino-monto-disponible"></span>
                        </div>

                        <div class="form-group">
                            <label>Tipo de documento</label>
                            <select class="form-control" id="select-tipo-documento">
                                <option value="O" selected="selected">Seleccionar...</option>
                                <option value="I">Inventario</option>
                                <option value="S">Servicio</option>
                            </select>
                        </div>

                        <div class="form-group" id="div-concepto-descuento">
                            <label>Concepto de descuento</label>
                            <select class="form-control" id="select-concepto-descuento"></select>
                        </div>

                        <div class="form-group">
                            <label for="">Comentario</label>
                            <textarea class="form-control" rows="2" id="comentario"></textarea>
                        </div>

                        <div id="loading-sap" style="display:none;" class="overlay">
                            <i class="fa  fa fa-refresh fa-spin"></i>
                        </div>
                        <button type="button" class="btn btn-info" id="btn-sap

">&nbsp&nbsp&nbsp<i class="fa fa-rocket">&nbsp&nbsp&nbsp</i></button>
                        <button type="button" class="btn btn-success pull-right" id="btn-enviar-cabeza" style="display:none">&nbsp&nbsp&nbsp<i class="fa fa-search">&nbsp&nbsp&nbsp</i></button>
                    </div>
                </form>
            </div>
        </div>
        <div class="col-md-8">
            <div class="box box-danger">
                <div class="box-header">
                    <i class="fa fa-credit-card"></i>
                    <h3 class="box-title">Nota de credito</h3>
                    <div id="loading" style="display:none;" class="overlay">
                        <i class="fa fa fa-refresh fa-spin"></i>
                    </div>
                </div>
                <div class="box-body">
                    <div class="table table-responsive">
                        <!-- TABLA FACTURA ORIGEN INVENTARIO -->
                        <table class="table table-bordered" id="tabla-factura-origen-inventario" style="width: 60%;">
                            <thead>
                                <tr>
                                    <th class="th-stationery" style="width: 20%;">Sku</th>
                                    <th class="th-stationery" style="width: 10%;">Solicitar</th>
                                    <th class="th-stationery" style="width: 10%;">Cantidad</th>
                                    <th class="th-stationery" style="width: 20%;">Precio</th>
                                </tr>
                            </thead>
                            <tbody id="tbody-factura-origen-inventario"></tbody>
                        </table>
                        <!-- TABLA FACTURA ORIGEN SERVICIO DESCUENTO-->
                        <table class="table table-bordered" id="tabla-factura-origen-servicio-descuento" style="width: 60%;">
                            <thead>
                                <tr>
                                    <th class="th-stationery">Sku</th>
                                    <th class="th-stationery">Solicitar</th>
                                    <th class="th-stationery">Cantidad</th>
                                    <th class="th-stationery">Precio</th>
                                    <th class="th-stationery">Descuento</th>
                                </tr>
                            </thead>
                            <tbody id="tbody-factura-origen-servicio-descuento"></tbody>
                        </table>
                    </div>
                    <!-- FORMULARIO NOTA DE CREDITO OTROS DESCUENTOS -->
                    <form class="form-horizontal" id="form-otro-concepto-descuento" name="myForm">
                        <div class="box-body">
                            <div class="form-group">
                                <label class="col-sm-2 control-label">Monto destino</label>
                                <div class="col-sm-10">
                                    <input type="text" class="form-control" id="input-factura-destino-monto-permitido-servicio-otro-descuento" disabled />
                                </div>
                            </div>
                            <div class="form-group">
                                <label class="col-sm-2 control-label">Monto solicitado sin IVA</label>
                                <div class="col-sm-10">
                                    <!--<input type="text" class="form-control number-only" id="input-factura-destino-monto-solicitado-servicio-otro-descuento-formato" onkeyup="validarCantidadPermitidaNotaCreditoServicioOtroDescuento()">-->
                                    <input type="text" class="form-control" id="input-factura-destino-monto-solicitado-servicio-otro-descuento" onkeyup="validarCantidadPermitidaNotaCreditoServicioOtroDescuento()">
                                </div>
                            </div>
                        </div>
                        <div class="box-footer">
                            <button type="button" class="btn btn-warning" id="boton-cancelar-nota-credito-servicio-otro-descuento"><i class="fa fa-times"></i></button>
                            <button type="button" class="btn btn-success pull-right" id="boton-solicitar-nota-credito-otro-servicio-descuento"><i class="fa fa-save"></i></button>
                        </div>
                    </form>

                </div>
            </div>
        </div>

        <div class="col-md-8">
            <div class="box box-danger">
                <div id="loading-nc" style="display:none;" class="overlay">
                    <i class="fa fa fa-refresh fa-spin"></i>
                </div>
                <div class="box-header">
                    <i class="fa fa-credit-card"></i>
                    <h3 class="box-title">Solicitud nota de credito</h3>
                    <div id="loading" style="display:none;" class="overlay">
                        <i class="fa  fa fa-refresh fa-spin"></i>
                    </div>
                </div>
                <div class="box-body">
                    <div class="table table-responsive">
                        <!-- TABLA NOTA DE CREDITO GENERADA PARA SOLICITUD -->
                        <table class="table table-bordered" id="tabla-nota-credito-solicitada-inventario" style="width: 80%;">
                            <thead>
                                <tr>
                                    <th class="th-stationery" style="width: 20%;">Sku</th>
                                    <th class="th-stationery" style="width: 10%;">Cantidad</th>
                                    <th class="th-stationery" style="width: 10%;">Precio</th>
                                    <th class="th-stationery" style="width: 10%;">Subtotal</th>
                                    <th class="th-stationery" style="width: 10%;">Remover</th>
                                </tr>
                            </thead>
                            <tbody id="tbody-nota-credito-solicitada-inventario"></tbody>
                            <tfoot>
                                <tr>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th id="subtotal-inventario" class="pull-right"></th>
                                    <th></th>
                                </tr>
                                <tr>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th id="iva-inventario" class="pull-right"></th>
                                    <th></th>
                                </tr>
                                <tr>
                                    <th></th>
                                    <th></th>
                                    <th>
                                        <button type="button" class="btn btn-warning" id="boton-cancelar-nota-credito-inventario"><i class="fa fa-times"></i></button>
                                        <button type="button" class="btn btn-success" id="boton-solicitar-nota-credito-inventario"><i class="fa fa-save"></i></button>
                                    </th>
                                    <th id="total-inventario" class="pull-right"></th>
                                    <th></th>
                                </tr>
                            </tfoot>
                        </table>
                        <!-- TABLA NOTA DE CREDITO GENERADA PARA SOLICITUD SERVICIO DESCUENTO-->
                        <table class="table table-bordered" id="tabla-nota-credito-solicitada-servicio-descuento" style="width: 100%;">
                            <thead>
                                <tr>
                                    <th class="th-stationery">Sku</th>
                                    <th class="th-stationery">Cantidad</th>
                                    <th class="th-stationery">Precio</th>
                                    <th class="th-stationery">Subtotal</th>
                                    <th class="th-stationery">Descuento</th>
                                    <th class="th-stationery">Descuento total</th>
                                    <th class="th-stationery">Remover</th>
                                </tr>
                            </thead>
                            <tbody id="tbody-nota-credito-solicitada-servicio-descuento"></tbody>
                            <tfoot>
                                <tr>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th id="subtotal-servicio-descuento" class="pull-right"></th>
                                    <th></th>
                                </tr>
                                <tr>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th></th>
                                    <th id="iva-servicio-descuento" class="pull-right"></th>
                                    <th></th>
                                </tr>
                                <tr>
                                    <th></th>
                                    <th></th>
                                    <th>
                                        <button type="button" class="btn btn-warning" id="boton-cancelar-nota-credito-servicio-descuento"><i class="fa fa-times"></i></button>
                                        <button type="button" class="btn btn-success" id="boton-solicitar-nota-credito-servicio-descuento"><i class="fa fa-save"></i></button>
                                    </th>
                                    <th></th>
                                    <th></th>
                                    <th id="total-servicio-descuento" class="pull-right"></th>
                                    <th></th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <!-- TABLA NOTAS DE CREDITO HISTORICO -->
        <div class="col-md-12">
            <div class="box box-danger">
                <div class="box-header">
                    <i class="fa fa-history"></i>
                    <h3 class="box-title">Historico Notas de Credito</h3>
                    <div id="loading" style="display:none;" class="overlay">
                        <i class="fa  fa fa-refresh fa-spin"></i>
                    </div>
                </div>
                <div class="box-body">
                    <div class="table table-responsive">
                        <table class="table table-striped" style="width:100%;" id="factura-origen">
                            <thead>
                                <tr>
                                    <th class="th-stationery">Sequence</th>
                                    <th class="th-stationery">Cliente</th>
                                    <th class="th-stationery">Folio Origen</th>
                                    <th class="th-stationery">Folio Destino</th>
                                    <th class="th-stationery">Tipo Documento</th>
                                    <th class="th-stationery">Concepto</th>
                                    <th style="min-width: 150px !important;" class="th-stationery">Comentario</th>
                                    <th class="th-stationery">Usuario</th>
                                    <th class="th-stationery">Estatus</th>
                                    <th class="th-stationery">Fecha</th>
                                    <th class="th-stationery">Folio SAP</th>
                                    <th class="th-stationery">Folio Pago SAP</th>
                                    <th class="th-stationery">Detalle</th>
                                    <th class="th-stationery">Comentarios</th>
                                    <th class="th-stationery">Evidencia</th>
                                </tr>
                            </thead>
                            <tbody id="table-invoice-history"></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- MODAL NOTA DE CREDITO INVENTARIO-->
    <div id="modal-nota-credito-inventario" class="modal fade" role="dialog">
        <div class="modal-dialog">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-body">
                        <div class="box-body">
                            <div class="row">
                                <div class="col-xs-12">
                                    <h2 class="page-header">
                                        <i class="fa fa-file-text"></i>&nbsp;&nbsp;&nbsp;Nota de crédito
                                        <small class="pull-right" id="set-Fecha"></small>
                                    </h2>
                                </div>
                            </div>
                            <div class="col-sm-6 invoice-col">
                                <address>
                                    <small id="set-Sequence"></small><br>
                                    <small id="set-Cliente"></small><br>
                                </address>
                            </div>
                            <div class="col-sm-6 invoice-col">
                                <address>
                                    <small id="set-FolioOrigen"></small><br>
                                    <small id="set-FolioDestino"></small><br>
                                    <small id="set-TipoDocumento"></small><br>
                                    <small id="set-Comentario"></small>
                                </address>
                            </div>
                            <div class="row">
                                <div class="col-xs-12 table-responsive">
                                    <table class="table table-striped">
                                        <thead>
                                            <tr>
                                                <th class="th-stationery">Sku</th>
                                                <th class="th-stationery">Cantidad</th>
                                                <th class="th-stationery" id="modal-precio">Precio</th>
                                                <th class="th-stationery" id="modal-descuento">Descuento</th>
                                                <th class="th-stationery" id="modal-descuento-total">Descuento total sin IVA</th>
                                            </tr>
                                        </thead>
                                        <tbody id="table-invoice-detail"></tbody>
                                        <tfoot align="right">

                                            <tr style="text-align:right">
                                                <td colspan="4">
                                                    <b>Total sin IVA:</b>
                                                </td>
                                                <td><span id="total-detail"></span></td>
                                            </tr>
                                        </tfoot>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <h1 id="estatus-rechazado"></h1>
                        <section class="main-timeline-section">
                            <div class="timeline-start"></div>
                            <div class="conference-center-line"></div>
                            <div class="conference-timeline-content">

                                <div class="timeline-article timeline-article-bottom">
                                    <div id="div-gestion-direccion" class="content-date">
                                        <small>Aprobación</small><br>
                                        <small>dirección</small>
                                    </div>
                                    <div class="meta-date" id="estatus-direccion"></div>
                                </div>

                                <div class="timeline-article timeline-article-bottom">
                                    <div id="div-gestion-credito" class="content-date">
                                        <small>Aprobación</small><br />
                                        <small>credito</small>
                                    </div>
                                    <div class="meta-date" id="estatus-credito"></div>
                                </div>

                                <div class="timeline-article timeline-article-bottom gestor-timeline-change">
                                    <div id="div-gestion-almacen" class="content-date">
                                        <small>Aprobación</small><br>
                                        <small>almacén</small>
                                    </div>
                                    <div class="meta-date" id="estatus-almacen"></div>
                                </div>

                                <div class="timeline-article timeline-article-bottom">
                                    <div id="div-gestion-gerencia" class="content-date">
                                        <small>Aprobación</small><br>
                                        <small>gerencia</small>
                                    </div>
                                    <div class="meta-date" id="estatus-gerencia"></div>
                                </div>

                            </div>

                            <div class="timeline-end"></div>
                        </section>
                        <br>
                        <br>
                        <hr>
                        <button type="button" class="btn btn-info pull-right" onclick="javascript:genScreenshot();"><i class="fa fa-fw fa-cloud-download"></i></button>
                        <a id="test"></a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- MODAL COMENTARIOS SOBRE LA NOTA DE CREDITO -->
    <div id="verComentarios" class="modal fade" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-body">
                    <div class="box-body">
                        <div class="row">
                            <div class="col-xs-12">
                                <h2 class="page-header">
                                    <i class="fa fa-file-text"></i>&nbsp;&nbsp;&nbsp;Comentarios nota de crédito
                                    <small class="pull-right" id="set-comentario"></small>
                                </h2>
                            </div>
                        </div>
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th class="th-stationery">Departamento</th>
                                    <th class="th-stationery">Usuario</th>
                                    <th class="th-stationery">Comentario</th>
                                </tr>
                            </thead>
                            <tbody id="tbody-comentarios"></tbody>
                        </table>
                    </div>
                    <form role="form">
                        <input id="Identifier_Comentario" type="text" style="display:none;" />
                        <div class="box-body">
                            <div class="form-group">
                                <label>Comentario </label>
                                <input type="text" multiple class="form-control" id="text-comentario" placeholder="Comentario">
                            </div>
                        </div>
                        <div class="box-footer">
                            <button id="btn-cancel-comentario" data-dismiss="modal" type="button" class="btn btn-default">Cancel</button>
                            <button id="btn-add-comentario" data-dismiss="modal" type="button"  class="btn btn-info pull-right">Guardar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div id="verImagenes" class="modal fade" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-body">
                    <div class="box-body">
                        <div class="row">
                            <div class="col-xs-12">
                                <h2 class="page-header">
                                    <i class="fa fa-file-text"></i>&nbsp;&nbsp;&nbsp;Evidencias de nota de crédito
                                </h2>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-sm-6 invoice-col">
                                <address>
                                    <small id="set-FolioNC"></small><br>
                                    <small id="set-ClienteNC"></small><br>
                                </address>
                            </div>
                            <div class="col-sm-6 invoice-col">
                                <address>
                                    <small id="set-FolioOrigenNC"></small><br>
                                    <small id="set-FolioDestinoNC"></small><br>
                                    <small id="set-TipoDocumentoNC"></small><br>
                                </address>
                            </div>
                        </div>
                        <div class="row">
                            <div class="box box-widget widget-user">
                                <div class="widget-user-image">
                                    <!--<img class="img-circle" src="../dist/img/boxed-bg.jpg" alt="User Avatar">-->


                                </div>
                                <div class="widget-user-header bg-blue">

                                    <div class="row" id="carrousel-dinamic">

                                    </div>
                                </div>

                            </div>
                        </div>

                    </div>

                    <form id="frm-addImagenes" method="post" class="form-horizontal" enctype="multipart/form-data">
                        <div class="box-body">
                            <div class="form-group">
                                <div class="row">
                                    <div class="box-body">
                                        <h2 class="page-header">
                                            <i class="fa fa-file-text"></i>&nbsp;&nbsp;&nbsp;Agregar evidencia
                                            <input type="hidden" id="NotaCreditoId" name="NotaCreditoId">
                                            <br />
                                            <small style="color:red !important">*Nota: Archivos admitido (Imagen y archivo PDF) con un tamaño maxico de 2 MB</small>
                                        </h2>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xs-12">
                            <div class="form-group">
                                <div id="Content-1" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-1" name="Evidencia-1" noEvidencia="1" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="1" id="deletEvidence1" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-2" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-2" name="Evidencia-2" noEvidencia="2" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="2" id="deletEvidence2" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-3" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-3" name="Evidencia-3" noEvidencia="3" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="3" id="deletEvidence3" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-4" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-4" name="Evidencia-4" noEvidencia="4" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="4" id="deletEvidence4" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-5" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-5" name="Evidencia-5" noEvidencia="5" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="5" id="deletEvidence5" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-6" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-6" name="Evidencia-6" noEvidencia="6" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="6" id="deletEvidence6" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-7" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-7" name="Evidencia-7" noEvidencia="7" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="7" id="deletEvidence7" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-8" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-8" name="Evidencia-8" noEvidencia="8" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="8" id="deletEvidence8" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                                <div id="Content-9" class="row"><div class="col-md-10"><input class="btn btn-default btn-block btn-evidencia" id="Evidencia-9" name="Evidencia-9" noEvidencia="9" type="file" accept="image/x-png,image/jpeg,application/pdf"></div><div class="col-md-2"><div Noinput="9" id="deletEvidence9" class="btn btn-danger btn-block btn-evidencia">X</div></div></div>
                            </div>
                        </div>
                        <div class="box-body">
                            <div class="col-md-12">
                                <div class="col-md-10">
                                </div>
                                <div class="col-md-2">
                                    <div class="row pull-right">
                                        <button id="btn-addevidencia" type="button" class="btn btn-primary btn-lg rigth-block">
                                            <i class="fa fa-plus"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>

            </div>

        </div>
    </div>


</section>

<script type="text/javascript">
    //  motrar

    $("#boton-solicitar-nota-credito-inventario").hide();
    $("#boton-solicitar-nota-credito-servicio-descuento").hide();
    $("#boton-solicitar-nota-credito-otro-servicio-descuento").hide();

    ShowBotonSAP();

    $('#btn-cancel-comentario').click(function (e) {
        loadComentarios();
    });
    function loadComentarios() {
        $('#text-comentario').val('');
        $('#Identifier_Comentario').val('');
        $('#verComentarios').hide();
    }


    $('#btn-add-comentario').click(function (e) {
        e.preventDefault();        

        let Identifier = $('#Identifier_Comentario').val();
        if (Identifier == '') {
            swal("Alerta !", "No se ha definido el la nota de crédito a agregar el comentario.", "warning");
            return false;
        }

        let Comentario = $('#text-comentario').val();
        if (Comentario == '') {
            swal("Comentario!", "Debe de escribir el comentario para continuar.", "warning");
            return false;
        }
        let EmailUser = localStorage.getItem('UserEmail');
        console.log('Email  ' + EmailUser);
        if (EmailUser == '') {
            swal("Usuarii!", "Se debe de especificar el usuario para guardar el comentario.", "warning");
            return false;
        }

        $.post('@Url.Action("AddNotaCreditoComentario", "NotaCredito")',
        {
            Identifier: Identifier,
            Comentario: Comentario,
            Usuario: EmailUser,
            Departamento : 20 @* USUARIO *@
        }, function (data) {
            if (data.Context != null) {
                if (data.Context) {
                    swal("Exitoso.", "Comentario agregado.", "success");
                }
                else {
                    swal("Alerta!", "Comentario no agregado", "warning");
                }
                $('#verComentarios').hide();
            }
            if (data.Message != 'Success') {
                alert('Error generado ' + data.Message);
            }
        }).done(function (data) {
        }).fail(function (data) {               
        }).always(function (data) {               
        });

    });


    $("#btn-addevidencia").click(function () {
        if($("#NotaCreditoId").val() != ""){
            var formData = new FormData($("#frm-addImagenes")[0]);
            $("#btn-addevidencia").attr("disabled", true);
				$.ajax({
					url: '@Url.Action("AddSolicitudArchivo", "Requerimiento")',
					type: "POST",
					data: formData,
					contentType: false,
					processData: false,
                    success: function (data) {
						if (data != null) {

                            if (data.Context != null) {
                                if (data.Context) {
                                    swal({
                                        title: 'OK',
                                        text: 'La evidencia se guardo correctamente',
                                        icon: 'success'
                                    });
                                    initModule2();
                                    $('#verImagenes').modal('hide');
                                }
                                else {
                                    swal({
                                        title: 'Error',
                                        text: 'Ha fallado al guardar la evidencia.',
                                        icon: 'Error'
                                    });
                                }
							}
							else
							{
								swal({
									title: 'Error!',
                                    text: 'Ocurrio un error al intentar guardar la evidencia. ' + data.Message,
									icon: 'Error'
								});
							}
						}
					}
				});
			}
        else{
				alert("No se ha seleccionado la nota de crédito a evidenciar.");
	    }
    });

    // FUNCION MUESTRA EL BOTON DE SAP PARA SINCRONIZAR LAS NOTAS DE CREDITO
    function ShowBotonSAP() {
        $("#btn-sap").css("display", "none");

        //let Rol = localStorage.getItem("Rol");
        //let UserEmail = localStorage.getItem("UserEmail");

        //if (UserEmail == "luis_diaz@fussionweb.com") {

        //    $("#btn-sap").css("display", "block");
        //}
        //else if (UserEmail == "juan_carlos@fussionweb.com") {

        //    $("#btn-sap").css("display", "none");
        //    $("#btn-sap").css("display", "block");
        //}
        //else {
        //    $("#btn-sap").css("display", "block");
        //    $("#btn-sap").css("display", "none");
        //}
    }

    // llenar el carrucel de la nota de credito

    function verImagenesNC(FolioNC) {
        initModule2();
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetImagenenNotaCredito", "NotaCredito")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"FolioNC":"' + FolioNC + '"}',
            dataType: "json",
        }).done(function (data) {
            if (data.Context != null) {
                $("#NotaCreditoId").val(data.Context.Identifier);
                $("#set-FolioNC").text("FOLIO SIE: " + data.Context.Identifier);
                $("#set-ClienteNC").text("CLIENTE: " + data.Context.CardName);
                $("#set-FolioOrigenNC").text("FOLIO ORIGEN: " + data.Context.FolioOrigen);
                $("#set-FolioDestinoNC").text("FOLIO DESTINO: " + data.Context.FolioDestino);
                $("#set-TipoDocumentoNC").text("TIPO DOCUMENTO: " + data.Context.TipoDocumento);

                // dibujar el carrucel
                $("#carrousel-dinamic").html('');
                var aux = 0;
                var cabeceras = '';
                var detalles = '';
                //llenamos las cabeceras y los detalles..
                if (data.Context.Imagenes.length <= 0) {
                    cabeceras = cabeceras + '<li data-target="#carousel-generic1" data-slide-to="0" class="active"></li>';
                    detalles = detalles + '<div class="item active"><img src="../dist/img/employee.jpg" /></div>';
                    aux++;
                }
                else {
                    $.each(data.Context.Imagenes, function (index, value) {
                        let tipo = value.Tipo;
                        //console.log(" Tipo: " + tipo);

                        if (aux == 0) {
                            cabeceras = cabeceras + '<li data-target="#carousel-generic1" data-slide-to="' + index + '" class="active"></li>';
                        } else {
                            cabeceras = cabeceras + '<li data-target="#carousel-generic1" data-slide-to="' + index + '"></li>';
                        }
                        if (aux == 0) {
                            if (tipo == 1) {
                                detalles = detalles + '<div class="item active"><img onclick="image(this)" class="img-responsive center-block" style="width:auto;" src="data:image/png;base64,' + value.ImagenBase64 + '" /> <div class="carousel-caption"><p>Usuario: ' + value.UserName + ' </p>   </div> </div > ';
                            }
                            else if (tipo == 2) {
                                detalles = detalles + '<div class="item active"><iframe align="center" style="width:600px; height:600px; max-height:800px; max-width: 400px;" src="data:application/pdf;base64,' + value.ImagenBase64 + '" /> <div class="carousel-caption"><p>Usuario: ' + value.UserName + ' </p>   </div> </div > ';
                            }
                        } else {
                            if (tipo == 1) {
                                detalles = detalles + '<div class="item"><img onclick="image(this)" class="img-responsive center-block" style="width:auto;" src="data:image/png;base64,' + value.ImagenBase64 + '" /><div class="carousel-caption"><p> Usuario: ' + value.UserName + '</p> <br/> </div> </div>';
                            }
                            else if (tipo == 2) {
                                detalles = detalles + '<div class="item"><iframe align="center" style="width:600px; height:600px; max-height:800px; max-width: 400px;" src="data:application/pdf;base64,' + value.ImagenBase64 + '" /> <div class="carousel-caption"><p>Usuario: ' + value.UserName + ' </p>   </div> </div > ';
                            }
                        }
                        aux++;
                    });
                }
                //se dibuja el carrusel
                $("#carrousel-dinamic").append(
                    "<div class='col-md-12' >" +
                        "<div id='carousel-generic1' class='carousel slide' data-ride='carousel' style='width:80%;margin-left:10%;'>" +
                        "<ol id='contenedor-carousel-cabecera' class='carousel-indicators'>" +
                        cabeceras +
                    "<\/ol>" +
                    "<div id='carousel-detalles' class='carousel-inner' role='listbox'>" +
                    detalles +
                    "<\/div>" +
                        "<a class='left carousel-control' href='#carousel-generic1' role='button' data-slide='prev'>" +
                        "<span class='glyphicon glyphicon-chevron-left' aria-hidden='true'><\/span>" +
                        "<span class='sr-only'>Previous<\/span>" +
                    "<\/a>" +
                    "<a class='right carousel-control' href='#carousel-generic1' role='button' data-slide='next'>" +
                    "<span class='glyphicon glyphicon-chevron-right' aria-hidden='true'><\/span>" +
                    "<span class='sr-only'>Next<\/span>" +
                    "<\/a>" +
                    "<\/div>" +
                    "<\/div>");

            }
            else {
                console.log("Data Null");
            }
            }).always(function (data) {

        }).fail(function (data) {
            });
    }


    $(document).on('change', 'input[type="file"]', function () {
        // this.files[0].size recupera el tamaño del archivo
        // alert(this.files[0].size);


        var fileName = this.files[0].name;
        var fileSize = this.files[0].size;

        if (parseInt(fileSize) > 3000000) {
            swal('Advertencia de tamaño de archivo','El archivo ' + fileName + ' no debe superar los 3MB', 'warning');
            this.value = '';
            this.files[0].name = '';
        } else {
            // recuperamos la extensión del archivo
            var ext = fileName.split('.').pop();

            // console.log(ext);
            switch (ext) {
                case 'jpg':
                case 'jpeg':
                case 'png':
                case 'pdf': break;
                default:
                    alert('El archivo no tiene la extensión adecuada');
                    this.value = ''; // reset del valor
                    this.files[0].name = '';
            }
        }
    });

    function image(img) {
        var src = img.src;
        window.open(src);
    }
    $("#btn-addevidencia").prop("disabled", true);
    function HideInputFile() {
        for (var i = 2; i < 10; i++) {
            var content = $("#Evidencia-" + i).val();
            if (content == '') {
                $("#Content-" + i).hide();
                $("#Evidencia-" + i).val('');
            }
        }
    }

    $(".btn-evidencia").change(function () {
        HideInputFile();
        var current = $(this).attr("noEvidencia");
        var next = Number(current) + 1;

        if (next > 1) {
            //btn-addevidencia
            $("#btn-addevidencia").prop("disabled", false);
        }
        else {
            $("#btn-addevidencia").prop("disabled", true);
        }
        $("#Content-" + next).show();
    });
    initModule2();

    function initModule2() {
        $("#Evidencia-1").val('');
        $("#Evidencia-2").val('');
        $("#Evidencia-3").val('');
        $("#Evidencia-4").val('');
        $("#Evidencia-5").val('');
        $("#Evidencia-6").val('');
        $("#Evidencia-7").val('');
        $("#Evidencia-8").val('');
        $("#Evidencia-9").val('');

        $("#Content-2").hide();
        $("#Content-3").hide();
        $("#Content-4").hide();
        $("#Content-5").hide();
        $("#Content-6").hide();
        $("#Content-7").hide();
        $("#Content-8").hide();
        $("#Content-9").hide();
    }

     //TRAE TODO EL EL HISTORICO DE NOTAS DE CREDITO SOLICITADAS POR USUARIO
    function getHistoricoNotaCredito() {
        let Email = localStorage.getItem('UserEmail');
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetNotaCreditoHistoryByUser", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"Email":"' + Email + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null && $.isArray(data.Context)) {

                    // "#table-invoice-history"
                    var VP = $('#factura-origen').DataTable();
                    VP.destroy();
                    $("#table-invoice-history").html('');

                    $.each(data.Context, function (index, value) {
                        let EstadoAprobacion = '';

                        if (value.TipoDocumento == 'S') {
                            if (value.Valor == 1 && value.Canal == 'RETAIL') {
                                //alert("Retail");
                                EstadoAprobacion = 'Aprobación pendiente por credito y cobranza';
                            }
                            else {
                                EstadoAprobacion = 'Aprobación pendiente por dirección';
                            }
                            value.TipoDocumento = 'Servicio';
                        }

                        if (value.TipoDocumento == 'I') {
                            value.TipoDocumento = 'Inventario';
                            if (value.Estatus == 1) {
                                EstadoAprobacion = 'Aprobación pendiente por el almacén'
                            }
                        }

                        if (value.Estatus == 0) {
                            if (value.ConceptoDescuento == 39) {
                                //alert("Pronto pago");
                                EstadoAprobacion = 'Aprobación pendiente por credito y cobranza';
                            }
                            else {
                                EstadoAprobacion = 'Aprobación pendiente por gerencia';
                            }
                        }

                        if (value.Estatus == 2) {
                            EstadoAprobacion = 'Aprobación pendiente por la dirección'
                        }
                        if (value.Estatus == 3) {
                            EstadoAprobacion = 'Autorizada'
                        }
                        if (value.Estatus == 4) {
                            EstadoAprobacion = 'SAP'
                        }
                        if (value.Estatus == 5) {
                            EstadoAprobacion = 'Rechazada';
                        }
                        if (value.Estatus == 10) {
                            EstadoAprobacion = 'No especificado';
                        }
                        $("#table-invoice-history").append(
                            "<tr>\
                                <td>" + value.Sequence + "</td>\
                                <td>" + value.CardCode + " - " + value.CardName + "</td>\
                                <td>" + value.FolioOrigen + "</td>\
                                <td>" + value.FolioDestino + "</td>\
                                <td>" + value.TipoDocumento + "</td>\
                                <td>" + value.Concepto + "</td>\
                                <td>" + value.Comentario + "</td>\
                                <td>" + value.Usuario + "</td>\
                                <td>" + EstadoAprobacion + "</td>\
                                <td>" + moment(value.Fecha).format("YYYY-MM-DD HH:mm:ss") + "</td>\
                                <td>" + value.FolioSap + "</td>\
                                <td>" + value.FolioPagoSap + "</td>\
                                <td align='center'>\
                                    <a class='btn btn-primary' data-target='#modal-nota-credito-inventario' data-toggle='modal' onclick='getHistoricoNotaCreditoDetail("+ value.Sequence+")' href= '#'>\
                                        <i class='fa fa-file-text'></i>\
                                    </a>\
                                </td>\
                                <td align='center'>\
                                    <a class='btn btn-info' data-target='#verComentarios' data-toggle='modal' onclick='verComentarios("+ value.Sequence + ")' data-id='" + value.Sequence + "' href= '#'>\
                                        <i class='fa fa-comment'></i>\
                                    </a>\
                                </td>\
                                <td align='center'>\
                                    <a class='btn btn-info' data-target='#verImagenes' data-toggle='modal' onclick='verImagenesNC("+ value.Sequence + ")' data-id='" + value.Sequence + "' href= '#'>\
                                        <i class='fa fa-eye'></i>\
                                    </a>\
                                </td>\
                                </tr>");
                    });

                    $('#factura-origen').DataTable({
                        "lengthMenu": [[10, 20, 30, -1], [10, 20, 30, "All"]],
                        dom: 'Bfrtip',
                        stateSave: true,
                        buttons: [
                            'excel'
                        ],
                        order: [[1, 'desc']],
                        "paging": true,
                        "searching": true,
                        "language": {
                            "url": "//cdn.datatables.net/plug-ins/1.10.13/i18n/Spanish.json"
                        }
                    });

                }
            },
            error: function () {
                swal("Error en la api: UsuarioFolio");
            }
        });
    }



    //RESETEA SELECT FORMULARIO
    function reseteaSelectsFormulario() {
        $("#select-tipo-documento option:eq(0)").prop("selected", true);
        $("#select-concepto-descuento option:eq(0)").prop("selected", true);
        $("#div-concepto-descuento").hide();
    }
    reseteaSelectsFormulario();
    //ESCONDE TODAS LOS TIPOS DE TABLA DE NOTA DE CREDITO TAMBIEN EL FORMULARIO OTROS DESCUENTOS
    function esconderTodasLasTablas() {
        $("#tabla-factura-origen-inventario").hide();
        $("#tabla-factura-origen-servicio-descuento").hide();
        $("#tabla-nota-credito-solicitada-inventario").hide();
        $("#tabla-nota-credito-solicitada-servicio-descuento").hide();
        $("#form-otro-concepto-descuento").hide();
    }
    esconderTodasLasTablas();
    //MUESTRA TABLA NOTA CREDITO ORIGEN INVENTARIO
    function mostrarTablasFacturaOrigenInventario() {
        $("#tabla-factura-origen-inventario").show();
        $("#tabla-nota-credito-solicitada-inventario").show();
    }
    //ESCONDE TABLA NOTA CREDITO ORIGEN INVENTARIO
    function esconderTablasFacturaOrigenInventario() {
        $("#tabla-factura-origen-inventario").hide();
        $("#tabla-nota-credito-solicitada-inventario").hide();
        $("#tbody-factura-origen-inventario").html("");
        $("#tbody-nota-credito-solicitada-inventario").html("");
    }
    //MUESTRA TABLA NOTA CREDITO ORIGEN SERVICIO DESCUENTO
    function mostrarTablasFacturaOrigenServicioDescuento() {
        $("#tabla-factura-origen-servicio-descuento").show();
        $("#tabla-nota-credito-solicitada-servicio-descuento").show();
    }
    //ESCONDE TABLA NOTA CREDITO ORIGEN SERVICIO DESCUENTO
    function esconderTablasFacturaOrigenServicioDescuento() {
        $("#tabla-factura-origen-servicio-descuento").hide();
        $("#tabla-nota-credito-solicitada-servicio-descuento").hide();
        $("#tbody-factura-origen-servicio-descuento").html("");
        $("#tbody-nota-credito-solicitada-servicio-descuento").html("");
    }
    //ESCONDE TABLA NOTA CREDITO OTROS DESCUENTOS
    function esconderFomularioOtrosDescuentos() {
        $("#form-otro-concepto-descuento").hide();
        $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val("");
        $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").val("");
    }
    //MUESTRA TABLA NOTA CREDITO OTROS DESCUENTOS
    function mostrarFomularioOtrosDescuentos() {
        $("#form-otro-concepto-descuento").show();
    }

    //CONSULTA FACTURA ORIGEN INVENTARIO O SERVICIO
    document.querySelector('#btn-enviar-cabeza').addEventListener('click', function (e) {
        e.preventDefault();

        let cadena_completa_cliente = $('#input-cliente').val();
        let factura_origen = $('#input-factura-origen').val();
        let factura_destino = $('#input-factura-destino').val();
        let tipo_documento = $('#select-tipo-documento option:selected').val();
        let concepto_descuento = $('#select-concepto-descuento').val();
        switch (tipo_documento) {
            case "I":
                if (cadena_completa_cliente == '' || factura_origen == '' || factura_destino == '') {
                    swal("!Faltan datos¡", "Faltan datos", "warning");
                } else {
                    //ESCONDE TABLAS DE INVENTARIO
                    esconderTablasFacturaOrigenServicioDescuento();
                    //ESCONDE FORMULARIO OTRO DESCUENTO
                    esconderFomularioOtrosDescuentos();
                    //MUESTRA TABLAS DE INVENTARIO
                    mostrarTablasFacturaOrigenInventario();
                    //API GetFacturaNC
                    consultaNotaCreditoOrigen(cadena_completa_cliente, factura_origen);
                    //API GetFacturaDestino
                    consultaFacturaDestino(factura_destino, cadena_completa_cliente, concepto_descuento);
                    //API GetFacturaNCValidacionLimiteDinero
                    getFacturaNCValidacionLimiteDinero(cadena_completa_cliente, factura_destino, factura_origen);
                }
                break;
            case "S":
                console.log("entro a consultar la factura origen");
                if (cadena_completa_cliente == '' || factura_origen == '' || concepto_descuento == '0') {
                    swal("!Faltan datos¡", "Faltan datos", "warning");
                }
                else {
                    switch (concepto_descuento) {
                        case "37":
                            if (cadena_completa_cliente == '' || factura_origen == '' || concepto_descuento == '0') {
                                swal("!Faltan datos¡", "Faltan datos", "warning");
                            } else {
                                //ESCONDE TABLAS DE SERVICIO DESCUENTO
                                esconderTablasFacturaOrigenInventario();
                                //ESCONDE FORMULARIO OTRO DESCUENTO
                                esconderFomularioOtrosDescuentos();
                                //MUESTRA TABLAS DE SEVICIO DESCUENTO
                                mostrarTablasFacturaOrigenServicioDescuento();
                                //API GetFacturaNC
                                consultaNotaCreditoOrigenServicioDescuento(cadena_completa_cliente, factura_origen);
                                //API GetFacturaDestino
                                consultaFacturaDestinoServicioDescuento(factura_destino, cadena_completa_cliente);
                                //API GetFacturaNCValidacionLimiteDinero
                                getFacturaNCValidacionLimiteDineroServicioDescuento(cadena_completa_cliente, factura_destino, factura_origen);
                            }
                            break;
                        default:
                            if (cadena_completa_cliente == '' || factura_origen == '' || concepto_descuento == '0') {
                                swal("!Faltan datos¡", "Faltan datos", "warning");
                            } else {
                                //ESCONDE TABLAS DE SERVICIO DESCUENTO
                                esconderTablasFacturaOrigenServicioDescuento();
                                //ESCONDE TABLAS DE INVENTARIO
                                esconderTablasFacturaOrigenInventario();
                                //MUESTRA FORMULARIO OTRO DESCUENTO
                                mostrarFomularioOtrosDescuentos();
                                //API GetFacturaNC
                                consultaNotaCreditoOrigenServicioOtroDescuento(cadena_completa_cliente, factura_origen);
                                //API GetFacturaDestino
                                consultaFacturaDestinoServicioOtroDescuento(factura_destino, cadena_completa_cliente);
                                //API GetFacturaNCValidacionLimiteDinero
                                getFacturaNCValidacionLimiteDineroServicioOtroDescuento(cadena_completa_cliente, factura_destino, factura_origen);
                            }
                            break;
                    }
                }
                break;
            default:
                swal("!Faltan datos¡", "Asegurese de poner datos correctos", "error");
        }
    });
    //GUARDA VALORES CLONADOS DE LA NOTA DE CREDITO TIPO INVENTARIO EN UN OBJETO
    var nc_row_inventario = [];
    function agregarNotaCreditoInventario(Codigo, Precio) {
        var res = {
            Codigo: Codigo,
            Precio: Precio
        }
        nc_row_inventario.splice(0, 0, res);
    }
    let count1 = 1;
    //CLONA EN TABLA VALORES DE LA NOTA DE CREDITO TIPO INVENTARIO
    function clonarTablaNotaDeCreditoInventario(Codigo, Precio, index, AcctCode, Cantidad) {

        let exist = 0;
        // count list
        let count = $(".sku_inventario");

        if (count.length > 0) {

            var aux = 0;
            $(".sku_inventario").each(function () {
                let sku = this.value;

                let cant1 = $("#cantidad-inventario-" + aux).val();

                if (Codigo == sku && cant1 == Cantidad) {
                    exist = 1;
                    swal("Registro duplicado", "El SKU " + Codigo + " ya se ha agregado anteriormente.", "warning");
                }
                aux++;
            });
        }
        if (exist == 0) {
            $("#tr-invoice-" + index).css("background-color", "#e6ffe6");
            $('#boton-cancelar-nota-credito-inventario').show();
            $('#boton-solicitar-nota-credito-inventario').show();
            $("#tbody-nota-credito-solicitada-inventario").show();
            agregarNotaCreditoInventario(Codigo, Precio);
            let Cantidad = $("#cantidad-inventario-" + index).val();
            let Cant = parseInt(Cantidad);
            let Pre = parseFloat(Precio);
            let SubTotal = Cant * Pre;
            let Sub = SubTotal;
            let subFormat = formatNumber.new(Sub.toFixed(3), "$ ");
            let preFormat = formatNumber.new(Pre.toFixed(3), "$ ");
            count1 = count1 + 1;
            var html_code = "";
            html_code += "<tr id='row" + count1 + "'>";
            html_code += "<td><input class='sku_inventario' type='text' name='itemcode[]' value='" + Codigo + "' class='itemcode-inventario' disabled/></td>";
            html_code += "<td><input type='text' name='cantidad[]' value='" + Cant + "' class='cantidad-inventario' disabled/></td>";
            html_code += "<td><input type='text' value='" + preFormat + "' name='pre-format[]' class='precio-inventario' disabled/></td>";
            html_code += "<td style='display:none'><input type='hidden' name='precio[]'   value='" + Pre + "'/></td>";
            html_code += "<td><input type='text' class='subtotal subtotal-inventario' name='sub-format[]' value='" + subFormat + "' disabled/></td>";
            html_code += "<td style='display:none'><input type='hidden' class='subtotal-hidden' value='" + Sub + "'/></td>";
            html_code += "<td style='display:none'><input type='text' name='acctcode[]' value='" + AcctCode + "'/></td>";
            html_code += "<td><button type='button' name='remove' data-row='row" + count1 + "' onclick='test(" + index + ")' class='btn btn-danger btn-sm remove'><i class='fa fa-trash'></i></button></td>";
            html_code += "</tr>";
            $("#tbody-nota-credito-solicitada-inventario").append(html_code);
            calculaMontoPermitidoNotaCreditoInventario();
        }

    }
    //CALCULA MONTO PERMITIDO NOTA DE CREDITO INVENTARIO
    function calculaMontoPermitidoNotaCreditoInventario() {
        var sum = 0;
        $(".subtotal-hidden").each(function () {
            let sumado = parseFloat(this.value);
            if (!isNaN(sumado) && sumado.length != 0) {
                sum += sumado;
            }
        });
        let subtotal = sum;
        let iva = sum * 0.16;
        let total = sum * 1.16;
        $('#iva-inventario').text("Iva: " + formatNumber.new(iva.toFixed(3), "$ "));
        $('#subtotal-inventario').text("Subtotal: " + formatNumber.new(subtotal.toFixed(3), "$ "));
        $('#input-factura-destino-monto-solicitado').val(total);
        $('#total-inventario').text("Total: " + formatNumber.new(total.toFixed(3), "$ "));
        let monto_permitido = $("#input-factura-destino-monto-disponible").val();
        let monto_solicitado = $('#input-factura-destino-monto-solicitado').val();
        let factura_destino_monto_permitido = parseFloat(monto_permitido);
        let factura_destino_monto_solicitado = parseFloat(monto_solicitado);

        if (factura_destino_monto_solicitado > (factura_destino_monto_permitido + 300)) {
            swal({
                title: "Solicitud",
                text: "El monto de la nota de credito original no puede ser superado.",
                icon: "error",
                button: "OK",
            });
            $('#row' + count).remove();
            //CALCULA MONTO PERMITIDO NOTA DE CREDITO INVENTARIO
            calculaMontoPermitidoNotaCreditoInventario();
        } else if (factura_destino_monto_solicitado == factura_destino_monto_permitido) {
            swal({
                title: "Solicitud",
                text: "Nota de credito (inventario) completada.",
                icon: "success",
                button: "OK",
            });
        }
    };
    //API GetFacturaNCValidacionLimiteDinero
    function getFacturaNCValidacionLimiteDinero(cadena_completa_cliente,factura_destino,factura_origen) {
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaNCValidacionLimiteDinero", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cadena_completa_cliente + '","FolioDestino":"' + factura_destino + '","FolioOrigen":"' + factura_origen + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null && $.isArray(data.Context)) {
                    let Origen = $("#input-factura-destino-monto-permitido").val();
                    // tipo de descuento
                    let Subtotal = data.Context[0].Subtotal;
                    let Resta = Origen - Subtotal;
                    if (Resta <= 0) {
                        Resta = 0.00;
                    }
                    let canal = data.Context[0].Canal;

                    $("#input-factura-destino-monto-disponible").val(Resta.toFixed(2));
                    $(".span-help-block-destino-monto-ocupado").text("Monto ocupado: " + formatNumber.new(Subtotal.toFixed(2), "$ "));
                    $(".span-help-block-destino-monto-disponible").text("Monto disponible: " + formatNumber.new(Resta.toFixed(2), "$ "));
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaNCValidacionLimiteDinero");
            }
        });
    }
    function test(index) {
        $("#tr-invoice-" + index).css("background-color", "#FFFFFF !important;");
    }
    //CALCULA RESTA MONTO PERMITIDO VS MONTO SOLICITADO NOTA DE CREDITO INVENTARIO
    $(document).on('click', '.remove', function () {
        var delete_row = $(this).data("row");
        $('#' + delete_row).remove();
        calculaMontoPermitidoNotaCreditoInventario();
    });
    // VALIDA LA CANTIDAD A SOLICITAR VS CANTIDAD PERMITIDA INVENTARIO
    function validarCantidadPermitidaNotaCreditoInventario(index) {
        var cantidad_solicitada = $("#cantidad-inventario-" + index).val();
        var cantidad_default = $("#cantidad-inventario-default-" + index).val();
        let cant_s = parseInt(cantidad_solicitada);
        let cant_d = parseInt(cantidad_default);
        if (cant_s > cant_d) {
            $('#cantidad-inventario-' + index).val(cant_d);
            swal({
                title: "Solicitud",
                text: "Supera la cantidad limite.",
                icon: "error",
                button: "OK",
            });
        }
    }
    //INSERTA SOLICITUD DE NOTA DE CREDITO TIPO INVENTARIO
    document.querySelector('#boton-solicitar-nota-credito-inventario').addEventListener('click', function (e) {
        e.preventDefault();
        swal({
            title: "¿Solicitar nota de credito?",
            text: "Se mandará solicitud de la nota de crédito (inventario).",
            icon: "warning",
            buttons: [
              'No, cancelar esta operación',
              'Si, estoy seguro'
            ],
            dangerMode: true,
        }).then(function (isConfirm) {
            if (isConfirm) {
                swal({
                    title: 'Operación confirmada',
                    text: 'Los datos se mandaron correctamente',
                    icon: 'success'
                }).then(function () {
                    let row_count_inventario = $('#tbody-nota-credito-solicitada-inventario >tr').length;
                    if (row_count_inventario == 0) {
                        swal("Seleccione al menos una partida");
                    } else {
                        //API InsertCabeceraNotaCredito InsertCuerpoNotaCredito
                        insertaNotaCreditoInventario();
                        //LIMPIA TODITO
                        limpiaTodito();
                        //LIMPIA TABLA table-invoice-history
                        $("#table-invoice-history").empty();
                        //API GetNotaCreditoHistoryByUser
                        getHistoricoNotaCredito();
                    }
                });
            } else {
                swal("Cancelado", "Operación cancelada", "error");
            }
        });
    });
    //API InsertCabeceraNotaCredito InsertCuerpoNotaCredito nota de credito inventario
    function insertaNotaCreditoInventario() {
        $('#loading-nc').show();
        let usuario = localStorage.getItem('UserEmail');
        let cliente = $('#input-cliente').val();
        let folio_origen = $('#input-factura-origen').val();
        let folio_destino = $('#input-factura-destino').val();
        let tipo_documento = $('#select-tipo-documento option:selected').val();
        let concepto_descuento = 38;
        let descuento = 0;
        let comentario = $('#comentario').val();
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetPermisosCanalByCardCode", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cliente + '"}',
            dataType: "json",
            success: function (data) {
                let canal = data.Context[0].Canal;
                let email = data.Context[0].Email;
                $.ajax({
                    cache: false,
                    contentType: "application/json; charset=utf-8",
                    url: '@Url.Action("InsertCabeceraNotaCredito", "Venta")',
                    async: false,
                    timeout: 3000,
                    type: 'POST',
                    data: '{"CardCode":"' + cliente + '","FolioOrigen":"' + folio_origen + '","FolioDestino":"' + folio_destino + '","TipoDocumento":"' + tipo_documento + '","Comentario":"' + comentario + '","Usuario":"' + usuario + '","ConceptoDescuento":"' + concepto_descuento + '","Canal":"' + canal + '","Email":"' + email + '"}',
                    dataType: "json",
                    success: function (data) {
                        let FolioNC = data.Context;
                       //alert(FolioNC);
                        if (data.Code == "-1" || concepto_descuento == '0') {
                            limpiaTodito();
                            $("#table-invoice-history").empty();
                            getHistoricoNotaCredito();
                            $('#loading-nc').hide();
                            swal("Favor de verificar la logistica del descuento.");
                        } else {
                            let itemcodes = document.getElementsByName('itemcode[]');
                            let cantidades = document.getElementsByName('cantidad[]');
                            let precios = document.getElementsByName('precio[]');
                            let cuentas = document.getElementsByName('acctcode[]');
                            for (var i = 0; i < itemcodes.length; i++) {
                                var itemcode = itemcodes[i];
                                var cantidad = cantidades[i];
                                var precio = precios[i];
                                var cuenta = cuentas[i];
                                $.ajax({
                                    cache: false,
                                    url: '@Url.Action("InsertCuerpoNotaCredito", "Venta")',
                                    async: false,
                                    timeout: 3000,
                                    method: "POST",
                                    data: { FolioNC: FolioNC, ItemCode: itemcode.value, Cantidad: cantidad.value, Precio: precio.value, Descuento: descuento, Cuenta: cuenta.value },
                                    success: function (data) {
                                        //limpiaTodito();
                                    }
                                });
                            }
                            //ENVIO DE EMAIL NOTA DE CREDITO INTVENTARIO

                            let ToEmail = "";
                            let ToEmail2 = "";
                            if (concepto_descuento == 39 && tipo_documento == "S") {
                                ToEmail = '@System.Configuration.ConfigurationManager.AppSettings["EmailCreditoCobranza"]';
                                ToEmail2= '@System.Configuration.ConfigurationManager.AppSettings["EmailSistemas"]';
                                //EmailCreditoCobranza
                            } else {
                                ToEmail = '@System.Configuration.ConfigurationManager.AppSettings["EmailSistemas"]';
                                ToEmail2 = '@System.Configuration.ConfigurationManager.AppSettings["EmailSistemas"]';
                                ToEmailGerenteInventarios = '@System.Configuration.ConfigurationManager.AppSettings["EmailGerenteInventarios"]';
                            }

                            //let ToEmail2 = 'armando_molina@fussionweb.com';
                            //let ToEmail3 = 'armando_molina@fussionweb.com';
                            //let ToEmail4 = 'armando_molina@fussionweb.com';

                            let Fecha = moment(new Date()).format('YYYY-MM-DD');

                            let FromEmail = '@System.Configuration.ConfigurationManager.AppSettings["EmailSistemasFussion"]';

                            let SubjectEmail = "NOTA DE CREDITO INVENTARIO";
                            let cliente_txt = $('#span-cliente').text();
                            if (tipo_documento == 'I') {
                                tipo_documento = 'Inventario';
                            } else if (tipo_documento == 'S') {
                                tipo_documento = 'Servicio';
                            }
                            var html_code = "<div>";
                            html_code += "<h4 style='font-family: arial, sans-serif;'>Cliente: " + cliente_txt + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Folio origen: " + folio_origen + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Folio destino: " + folio_destino + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Tipo: " + tipo_documento + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Comentario: " + comentario + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px 0px 15px 0px; padding: 0px 0px 15px 0px;'>Fecha: " + Fecha + "</h4>";
                            html_code += "<table style='font-family: arial, sans-serif; border-collapse: collapse; width: 100%;'>";
                            html_code += "<thead>";
                            html_code += "<tr style='text-align: center; padding: 8px; background-color: rgb(221, 75, 57); color:#FFFFFF'>";
                            html_code += "<th>SKU</th>";
                            html_code += "<th>Cantidad</th>";
                            html_code += "<th>Precio</th>";
                            html_code += "<th>Subtotal</th>";
                            html_code += "</tr>";
                            html_code += "</thead>";
                            html_code += "<tbody style='border: 2px solid black; border-collapse: separate; border-spacing: 4px; text-align: center'>";
                            let precio_formateados = document.getElementsByName('pre-format[]');
                            let subtotal_formateados = document.getElementsByName('sub-format[]');
                            var total = 0;
                            for (var i = 0; i < itemcodes.length; i++) {
                                var itemcode = itemcodes[i];
                                var cantidad = cantidades[i];
                                var precio = precios[i];
                                var precio_formateado = precio_formateados[i];
                                var subtotal_formateado = subtotal_formateados[i];
                                var subtotal = parseFloat(cantidad.value) * parseFloat(precio.value);
                                var total = total + subtotal;
                                html_code += "<tr style='text-align: center'>";
                                html_code += "<td>" + itemcode.value + "</td>";
                                html_code += "<td>" + cantidad.value + "</td>";
                                html_code += "<td>" + precio_formateado.value + "</td>";
                                html_code += "<td>" + subtotal_formateado.value + "</td>";
                                html_code += "</tr>";
                            }
                            html_code += "</tbody>";
                            html_code += "</table>";
                            let con_iva = total * 0.16;
                            let totalx = total * 1.16;
                            let iva = total * 0.16;
                            html_code += "<h4 style='font-family: arial, sans-serif; text-align: right;'>SUBTOTAL SIN IVA: " + formatNumber.new(total.toFixed(2), "$ ") + "</h4>";
                            //html_code += "<h4 style='font-family: arial, sans-serif; text-align: right;'>IVA: " + formatNumber.new(iva.toFixed(2), "$ ") + "</h4>";
                            //html_code += "<h4 style='font-family: arial, sans-serif; text-align: right;'>TOTAL: " + formatNumber.new(totalx.toFixed(2), "$ ") + "</h4>";
                            html_code += "</div>";
                            $.ajax({
                                contentType: "application/json; charset=utf-8",
                                url: '@Url.Action("EmailSendNotaCredito", "Venta")',
                                async: false,
                                timeout: 3000,
                                type: 'POST',
                                data: '{"ToEmail":"' + ToEmail + '","ToEmail2":"' + ToEmailGerenteInventarios + '","ToEmail3":"' + ToEmail2 + '","ToEmail4":"' + ToEmail2 + '","FromEmail":"' + FromEmail + '","SubjectEmail":"' + SubjectEmail + '","BodyEmail":"' + html_code + '","Cliente":"' + cliente_txt + '"}',
                                dataType: "json",
                                success: function (data) {
                                },
                                error: function () {
                                    alert("Error en la api: EmailListPrice");
                                }
                            });
                            $('#loading-nc').hide();
                        }
                    },
                    error: function () {
                        swal("Error en la api: InsertCabeceraNotaCredito");
                    }
                });
            },
            error: function () {
                alert("Error en la api: GetFacturaNC");
            }
        });
    }
                                            /*FIN NOTA DE CREDITO INVENTARIO*/
    /*++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
    ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++*/
    //TIPO DE DOCUMENTO SWITCH CONCEPTO DE DESCUENTO
    // CAMBIA DE SELECION
    $("#select-concepto-descuento").change(function () {

        // validar que no permita agregar un concepto repetido
        let cadena_completa_cliente = $('#input-cliente').val();
        let factura_origen = $('#input-factura-origen').val();
        let factura_destino = $('#input-factura-destino').val();
        let tipo_documento = $('#select-tipo-documento option:selected').val();
        let concepto_descuento = $('#select-concepto-descuento').val();


        // valor
        let valor_tipo_descuento = document.getElementById("select-concepto-descuento").value;




        if (cadena_completa_cliente == '' || factura_origen == '' || concepto_descuento == '0') {
            swal("!Faltan datos¡", "Faltan datos", "warning");
        }
        else {
            // validar el concepto no exista en

            $.ajax({
                contentType: "application/json; charset=utf-8",
                url: '@Url.Action("GetValidaConceptoDescuentoNC", "Venta")',
                async: false,
                timeout: 3000,
                type: 'POST',
                data: '{"Cliente":"' + cadena_completa_cliente + '","FolioOrigen":"' + factura_origen + '","FolioDestino":"' + factura_destino + '","Concepto":"' + valor_tipo_descuento + '"}',
                dataType: "json",
                success: function (data) {
                    if (data != null) {
                        let exist = data.Context;

                        if (exist > 0) {
                            swal("!Advertencia¡", "La nota de crédito que desea ingresar ya existe.", "warning");
                        }
                        else {
                            switch (valor_tipo_descuento) {
                                case '37': // diferiencia de precio 100%
                                    let cadena_completa_cliente = $('#input-cliente').val();
                                    let factura_origen = $('#input-factura-origen').val();
                                    let factura_destino = $('#input-factura-destino').val();
                                    let tipo_documento = $('#select-tipo-documento option:selected').val();
                                    let concepto_descuento = $('#select-concepto-descuento').val();
                                    if (cadena_completa_cliente == '' || factura_origen == '' || concepto_descuento == '0') {
                                        swal("!Faltan datos¡", "Faltan datos", "warning");
                                    } else {
                                        //ESCONDE TABLAS DE SERVICIO DESCUENTO
                                        esconderTablasFacturaOrigenInventario();
                                        //ESCONDE FORMULARIO OTRO DESCUENTO
                                        esconderFomularioOtrosDescuentos();
                                        //MUESTRA TABLAS DE SEVICIO DESCUENTO
                                        mostrarTablasFacturaOrigenServicioDescuento();
                                        //API GetFacturaNC
                                        // monto origen
                                        consultaNotaCreditoOrigenServicioDescuento(cadena_completa_cliente, factura_origen);
                                        //API GetFacturaDestino
                                        //monto  destino
                                        consultaFacturaDestinoServicioDescuento(factura_destino, cadena_completa_cliente);
                                        //API GetFacturaNCValidacionLimiteDinero
                                        getFacturaNCValidacionLimiteDineroServicioDescuento(cadena_completa_cliente, factura_destino, factura_origen);
                                    }
                                    break;

                                default:
                                    let cadena_completa_clientex = $('#input-cliente').val();
                                    let factura_origenx = $('#input-factura-origen').val();
                                    let factura_destinox = $('#input-factura-destino').val();
                                    //ESCONDE TABLAS DE SERVICIO DESCUENTO
                                    esconderTablasFacturaOrigenServicioDescuento();
                                    //ESCONDE TABLAS DE INVENTARIO
                                    esconderTablasFacturaOrigenInventario();
                                    //MUESTRA FORMULARIO OTRO DESCUENTO
                                    mostrarFomularioOtrosDescuentos();
                                    //API GetFacturaNC
                                    consultaNotaCreditoOrigenServicioOtroDescuento(cadena_completa_clientex, factura_origenx);
                                    //API GetFacturaDestino
                                    consultaFacturaDestinoServicioOtroDescuento(factura_destinox, cadena_completa_clientex);
                                    //API GetFacturaNCValidacionLimiteDinero
                                    getFacturaNCValidacionLimiteDineroServicioOtroDescuento(cadena_completa_clientex, factura_destinox, factura_origenx);
                            }
                        }
                    }
                },
                error: function () {
                    alert("Error en la api: GetValidaConceptoDescuentoNC");
                }
            });
        }

    });
    //TIPO DE DOCUMENTO SWITCH CONCEPTO DE DESCUENTO
    $("#select-tipo-documento").change(function () {
        let valor_tipo_documento = document.getElementById("select-tipo-documento").value;

        let factura_destino = $('#input-factura-destino').val();
        // generar un alerta de que no encontro la nota de credito destino
        if (factura_destino == '') {
            swal("!Debe de especificar la factura destino a aplicar la nota de crédito¡", "Faltan datos", "warning");
        }
        else {
            // validar la factura destino extista

            switch (valor_tipo_documento) {
                case 'I':
                    $("#div-concepto-descuento").hide();
                    $("#btn-enviar-cabeza").click();
                    GetNotaCreditoInventario();
                    break;
                case 'S':
                    esconderTodasLasTablas();
                    $("#div-concepto-descuento").show();
                    getConceptosServicios();
                    break;
                default:
                    console.log('Default switch');
            }
        }
    });
    // VALIDA LA CANTIDAD A SOLICITAR VS CANTIDAD PERMITIDA SERVICIO DESCUENTO
    function validarCantidadPermitidaNotaCreditoServicioDescuento(index) {
        var cantidad_solicitada = $("#cantidad-servicio-descuento-" + index).val();
        var cantidad_default = $("#cantidad-default-servicio-descuento-" + index).val();
        let cant_s = parseInt(cantidad_solicitada);
        let cant_d = parseInt(cantidad_default);
        if (cant_s > cant_d) {
            $("#cantidad-servicio-descuento-" + index).val(cant_d);
            swal({
                title: "Solicitud",
                text: "Supera la cantidad limite.",
                icon: "error",
                button: "OK",
            });
        }
    }
    //VALIDA EL DESCUENTO GENERADO POR EL 100 %
    function validarDescuentoPermitidoServicioDescuento(index, Precio) {
        let descuento_permitido = $('#input-descuento-permitido-servicio-descuento-' + index).val();
        let descuento_solicitado = $('#input-descuento-solicitado-servicio-descuento-' + index).val();
        let cant_d = parseFloat(descuento_permitido);
        let cant_s = parseFloat(descuento_solicitado);
        if (cant_s > cant_d) {
            $('#input-descuento-solicitado-servicio-descuento-' + index).val(cant_d);
            swal({
                title: "Solicitud",
                text: "Supera el descuento permitido.",
                icon: "error",
                button: "OK",
            });
        }
    }
    //API GetFacturaNC SERVICIO DESCUENTO
    function consultaNotaCreditoOrigenServicioDescuento(cadena_completa_cliente, factura_origen){
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaNC", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cadena_completa_cliente + '","DocNum":"' + factura_origen + '"}',
            dataType: "json",
            success: function (data) {
                $('#tbody-factura-origen-servicio-descuento').empty();
                if (data != null && $.isArray(data.Context)) {
                    // rescato el valor de la factura origen
                    //let PrecioFacturaOrigen = data.Context.Precio;
                    //alert(PrecioFacturaOrigen);

                    if (JSON.stringify(data.Context) == "[]") {
                        $("#div-custom-origen").removeClass("has-success").addClass("has-error");
                        $(".span-help-block-origen").text("Folio origen no disponible");
                        //alert("Verificar el folio destino sea correcto. No se encontraron referencias de la factura destino verificar datos ingresados");
                    }
                    else {
                        $("#div-custom-origen").removeClass("has-error").addClass("has-success");
                        $(".span-help-block-origen").text("Folio origen disponible");
                        $.each(data.Context, function (index, value) {

                            /******************************* DIFERENCIA EN PRECIO HARD CORE ***********************************************************************/
                            let descuento_fijo = 1 * value.Precio;
                            $("#tbody-factura-origen-servicio-descuento").append(
                                "<tr id='tr-servicio-descuento-"+index+"'>\
                                    <td class='color-sku' ondblclick='return clonarTablaNotaDeCreditoServicioDescuento(\"" + value.Codigo + "\",\"" + value.Precio+ "\",\"" + index + "\")'>" + value.Codigo + "</td>\
                                    <td><input type='text' class='cantidad-servicio-descuento' id='cantidad-servicio-descuento-"+ index + "' min='1' value='" + value.Cantidad + "' onkeyup='validarCantidadPermitidaNotaCreditoServicioDescuento(" + index +")' onkeypress='return isNumber(event)'/></td>\
                                    <td><input type='text' class='cantidad-default-servicio-descuento' id='cantidad-default-servicio-descuento-"+ index + "' value='" + value.Cantidad + "' disabled></td>\
                                    <td>" + formatNumber.new(value.Precio.toFixed(2), "$ ") + "</td>\
                                    <td style='display:none'>"+ 1 + "</td>\
                                    <td style='display:none'><input type='text' id='input-descuento-permitido-servicio-descuento-"+ index + "' value='" + descuento_fijo.toFixed(2) + "' disabled></td>\
                                    <td><input type='text' class='input-descuento-solicitado-servicio-descuento' id='input-descuento-solicitado-servicio-descuento-" + index + "' onclick='validarDescuentoPermitidoServicioDescuento(\"" + index + "\",\"" + value.Precio + "\")' onkeyup='validarDescuentoPermitidoServicioDescuento(\"" + index + "\",\"" + value.Precio + "\")' value='0'/></td>\
                                </tr>");
                        });
                    }
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaNC");
            }
        });
    }
    //GUARDA VALORES CLONADOS DE LA NOTA DE CREDITO TIPO SERVICIO DESCUENTO EN UN OBJETO
    var nc_row_servicio_descuento = [];
    function agregarNotaCreditoServicioDescuento(Codigosd, Preciosd) {
        var ressd = {
            Codigosd: Codigosd,
            Preciosd: Preciosd
        }
        nc_row_servicio_descuento.splice(0, 0, ressd);
    }
    var countsd = 0;
    //CLONA EN TABLA VALORES DE LA NOTA DE CREDITO TIPO SERVICIO DESCUENTO
    function clonarTablaNotaDeCreditoServicioDescuento(Codigosd, Preciosd, indexsd) {
        //alert(indexsd);
        let index_Aux = parseInt(indexsd);
        let exist = 0;
        let countSKU = $(".sku");
        if (countSKU.length > 0) {
            $(".sku").each(function () {

                let sku = this.value;
                if (Codigosd == sku) {
                    exist = 1;
                    swal("Registro duplicado", "El SKU " + Codigosd + " ya se ha agregado anteriormente.", "warning");
                }
            });
        }

        if (exist == 0) {

            let Descuentosdv = $("#input-descuento-solicitado-servicio-descuento-" + indexsd).val();
            parseFloat(Descuentosdv);
            if (Descuentosdv == 0) {
                swal("Faltan valores", "Favor de revisar el descuento aplicado", "warning");
            } else {
                // cambia de color el index que esta seleccionado
                $("#tr-servicio-descuento-" + indexsd).css("background-color", "#e6ffe6");

                $('#boton-cancelar-nota-credito-servicio-descuento').show();
                $('#boton-solicitar-nota-credito-servicio-descuento').show();
                $("#tbody-nota-credito-solicitada-servicio-descuento").show();

                let Cantidadsd = $("#cantidad-servicio-descuento-" + indexsd).val();

                let Descuentosd = $("#input-descuento-solicitado-servicio-descuento-" + indexsd).val();

                let DescuentosdFormat = formatNumber.new(Descuentosd + ".00", "$ ");

                let Cantsd = parseInt(Cantidadsd);
                let Presd = parseFloat(Preciosd);
                let SubTotalsd = Cantsd * Presd;
                let Subsd = parseFloat(SubTotalsd);
                let subFormatsd = formatNumber.new(Subsd.toFixed(2), "$ ");
                let preFormatsd = formatNumber.new(Presd.toFixed(2), "$ ");
                let DescuentoTotal = Cantsd * parseFloat(Descuentosd);
                let DescuentoTotalsdFormat = formatNumber.new(DescuentoTotal.toFixed(2), "$ ");

                countsd = countsd + 1;

                var html_code1 = "";
                html_code1 += "<tr id='row" + countsd + "'>";
                html_code1 += "<td><input class='sku' type='text' name='itemcode_sd[]'   value='" + Codigosd + "' style='width: 100px' disabled/></td>";
                html_code1 += "<td><input type='text' name='cantidad_sd[]' value='" + Cantsd + "' style='width: 60px'  disabled/></td>";
                html_code1 += "<td><input class='precio-sd' type='text' value='" + preFormatsd + "' name='pre-formatsd[]' disabled/></td>";
                html_code1 += "<td style='display:none'><input type='hidden' name='precio_sd[]' value='" + Presd + "' style='width: 50px'/></td>";
                html_code1 += "<td><input type='text' class='subtotal subtotal-sd' name='sub-formatsd[]' value='" + subFormatsd + "' disabled/></td>";
                html_code1 += "<td style='display:none'><input type='text' name='descuento_sd[]'  value='" + Descuentosd + "' style='width: 50px' disabled/></td>";
                html_code1 += "<td style='display:none'><input type='text' class='subtotal'  value='" + DescuentoTotal + "' disabled/></td>";
                html_code1 += "<td><input class='descuento-sd' name='des-formatsd[]' type='text' value='" + DescuentosdFormat + "' disabled/></td>";
                html_code1 += "<td><input class='total-sd' name='total-formatsd[]' type='text' value='" + DescuentoTotalsdFormat + "' disabled/></td>";
                html_code1 += "<td style='display:none'><input type='hidden' class='subtotal-servicio-descuento-hidden' value='" + Subsd + "' style='width: 50px'/></td>";
                html_code1 += "<td><button type='button' name='remove' data-row='row" + countsd + "' onclick='test2(" + indexsd + ")' class='btn btn-danger btn-sm removesd'><i class='fa fa-trash'></i></button></td>";
                html_code1 += "</tr>";

                $("#tbody-nota-credito-solicitada-servicio-descuento").append(html_code1);
                $("#boton-solicitar-nota-credito-servicio-descuento").show();
                calculaMontoPermitidoNotaCreditoServicioDescuento();
            }

        }

    }
    //CALCULA MONTO PERMITIDO NOTA DE CREDITO SERVICIO DESCUENTO
    function calculaMontoPermitidoNotaCreditoServicioDescuento() {
        var sumsd = 0;
        $(".subtotal").each(function () {
            let sumadosd = this.value;
            if (!isNaN(sumadosd) && sumadosd.length != 0) {
                sumsd += parseFloat(sumadosd);
            }
        });
        let totalsd = sumsd * 1.16;
        let ivasd = sumsd * 0.16;
        let subtotalsd = sumsd;
        $('#iva-servicio-descuento').text("Iva: " + formatNumber.new(ivasd.toFixed(2), "$ "));
        $('#subtotal-servicio-descuento').text("Subtotal: " + formatNumber.new(subtotalsd.toFixed(2), "$ "));
        $('#input-factura-destino-monto-solicitado-servicio-descuento').val(subtotalsd);
        $('#total-servicio-descuento').text("Total: " + formatNumber.new(totalsd.toFixed(2), "$ "));
        let monto_permitidosd = $("#input-factura-destino-monto-disponible-servicio-descuento").val();
        let monto_solicitadosd = $('#input-factura-destino-monto-solicitado-servicio-descuento').val();
        let factura_destino_monto_permitidosd = parseFloat(monto_permitidosd);
        let factura_destino_monto_solicitadosd = parseFloat(monto_solicitadosd).toFixed(2);
        if (factura_destino_monto_solicitadosd > factura_destino_monto_permitidosd) {
            swal({
                title: "Solicitud",
                text: "El monto de la nota de credito original no puede ser superado.",
                icon: "error",
                button: "OK",
            });
            $('#row' + countsd).remove();
            //CALCULA MONTO PERMITIDO NOTA DE CREDITO SERVICIO DESCUENTO
            //calculaMontoPermitidoNotaCreditoServicioDescuento();
        } else if (factura_destino_monto_solicitadosd == factura_destino_monto_permitidosd) {
            swal({
                title: "Solicitud",
                text: "Nota de credito (inventario) completada.",
                icon: "success",
                button: "OK",
            });
        }
    };
    function test2(indexsd) {
        //alert(indexsd);
        $("#tr-servicio-descuento-"+indexsd).css("background-color", "#FFFFFF !important;");
    }
    //CALCULA RESTA MONTO PERMITIDO VS MONTO SOLICITADO NOTA DE CREDITO SERVICIO DESCUENTO
    $(document).on('click', '.removesd', function () {
        var delete_rowsd = $(this).data("row");
        $('#' + delete_rowsd).remove();
        //CALCULA MONTO PERMITIDO NOTA DE CREDITO SERVICIO DESCUENTO
        calculaMontoPermitidoNotaCreditoServicioDescuento();
    });

    //API GetFacturaDestino servicio descuento
    function consultaFacturaDestinoServicioDescuento(factura_destino, cadena_completa_cliente) {

        //if (cadena_completa_cliente == 'C002863') {// tiendas soriana aplica
        //    alert("Cliente " + cadena_completa_cliente);
        //    factura_destino = $('#input-factura-origen').val(); // debe de tomar la factura origen para la validacion
        //}

        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaDestino", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"DocNum":"' + factura_destino + '","CardCode":"' + cadena_completa_cliente + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null && $.isArray(data.Context)) {
                    if (JSON.stringify(data.Context) == "[]") {

                        $("#div-custom-destino").removeClass("has-success").addClass("has-error");
                        $(".span-help-block-monto-destino").text("Folio destino no disponible");
                        $(this).next(".span-help-block-monto-destino").css("display", "inline").fadeOut(1000);
                        alert("!Favor de validar el folio destino. ya que no se encuentraron referencias del folio destino¡");
                    } else {
                        let canal = data.Context[0].Canal;

                        let monto_facturado = data.Context[0].MontoFactura;


                        // monto de la factura origen
                        let monto_factura_origen = data.Context[0].MontoFacturaOrigen;

                        let concepto_descuento = $("#select-concepto-descuento option:selected").val();

                        if (canal == 'RETAIL') {

                            $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                            $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(monto_factura_origen.toFixed(2), "$ "));
                            $("#input-factura-destino-monto-permitido-servicio-descuento").val(monto_factura_origen);
                        }
                        else {

                            if (concepto_descuento == '39' && canal == 'MAYOREO') {
                                $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(monto_factura_origen.toFixed(2), "$ "));
                                $("#input-factura-destino-monto-permitido-servicio-descuento").val(monto_factura_origen);
                            }
                            // 91 garantias sie daniel
                            // 93 garantias sie productivo
                            else if (concepto_descuento == '@System.Configuration.ConfigurationManager.AppSettings["ValueGarantias2"]' && canal == 'MAYOREO') {

                                $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(monto_factura_origen.toFixed(2), "$ "));
                                $("#input-factura-destino-monto-permitido-servicio-descuento").val(monto_factura_origen);
                            }
                            else {
                                //console.log("Otro descuento " + monto_facturado);
                                $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(monto_facturado.toFixed(2), "$ "));
                                $("#input-factura-destino-monto-permitido-servicio-descuento").val(monto_facturado);
                            }
                        }

                        //let concepto_descuento = $("#select-concepto-descuento option:selected").val();
                        $.ajax({
                            contentType: "application/json; charset=utf-8",
                            url: '@Url.Action("GetPorcentajePorIdConceptoServicio", "Venta")',
                            async: false,
                            timeout: 3000,
                            type: 'POST',
                            data: '{"Sequence":"' + concepto_descuento + '"}',
                            dataType: "json",
                            success: function (data) {
                                if (data != null && $.isArray(data.Context)) {
                                    let Cuenta = data.Context[0].Cuenta;

                                    $("#input-factura-destino-cuenta-servicio-descuento-hidden").val(Cuenta);
                                    //alert($("#input-factura-destino-cuenta-servicio-descuento-hidden").val());
                                }
                            },
                            error: function () {
                                alert("Error en la api: GetPorcentajePorIdConceptoServicio");
                            }
                        });
                    }
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaDestino");
            }
        });
    }
    //API GetFacturaNCValidacionLimiteDinero SERVICIO DESCUENTO
    function getFacturaNCValidacionLimiteDineroServicioDescuento(cadena_completa_cliente,factura_destino,factura_origen) {
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaNCValidacionLimiteDinero", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cadena_completa_cliente + '","FolioDestino":"' + factura_destino + '","FolioOrigen":"' + factura_origen + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null) {

                    if (data.Context.Subtotal != 0.00 && data.Context.Canal != 'NO') {
                        let origenFactura = 0, Subtotal = 0, Resta = 0;

                        let canal = data.Context[0].Canal;
                        let Origen = $("#input-factura-destino-monto-permitido-servicio-descuento").val();
                        // rescato para calcular del canal de retail
                        if (canal == 'RETAIL') {
                            //let origenFactura = $("#input-factura-destino-monto-permitido-servicio-descuento").val();
                            origenFactura = $(".span-help-block-monto-destino").text();
                            //alert(origenFactura);
                            Subtotal = origenFactura;
                            Resta = Origen - Subtotal;
                        }
                        else {
                            Subtotal = data.Context[0].Subtotal;
                            Resta = Origen - Subtotal;
                        }

                        $("#input-factura-destino-monto-disponible-servicio-descuento").val(Resta.toFixed(2));
                        $(".span-help-block-destino-monto-ocupado").text("Monto ocupado: " + formatNumber.new(Subtotal.toFixed(2), "$ "));
                        $(".span-help-block-destino-monto-disponible").text("Monto disponible: " + formatNumber.new(Resta.toFixed(2), "$ "));
                    }
                }
                else {
                    console.log("Error Data null");
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaNCValidacionLimiteDinero");
            }
        });
    }
    //INSERTA SOLICITUD DE NOTA DE CREDITO TIPO SERVICIO DESCUENTO
    document.querySelector('#boton-solicitar-nota-credito-servicio-descuento').addEventListener('click', function (e) {
        e.preventDefault();
        swal({
            title: "¿Solicitar nota de credito?",
            text: "Se mandara solicitud de la nota de credito (servicio descuento).",
            icon: "warning",
            buttons: [
                'No, cancelar esta operación',
                'Si, estoy seguro'
            ],
            dangerMode: true,
        }).then(function (isConfirm) {
            if (isConfirm) {
                swal({
                    title: 'Operación confirmada',
                    text: 'Los datos se mandaron correctamente',
                    icon: 'success'
                }).then(function () {
                    let row_count_servicio_descuento = $('#tbody-nota-credito-solicitada-servicio-descuento').length;
                    if (row_count_servicio_descuento == 0) {
                        swal("Seleccione al menos una partida");
                    } else {
                        //API InsertCabeceraNotaCredito InsertCuerpoNotaCredito nota de credito servicio descuento
                        insertaNotaCreditoServicioDescuento();
                        //LIMPIA TODITO
                        limpiaTodito();
                        //LIMPIA TABLA table-invoice-history
                        $("#table-invoice-history").empty();
                        //API GetNotaCreditoHistoryByUser
                        getHistoricoNotaCredito();
                    }
                });
            } else {
                swal("Cancelado", "Operación cancelada", "error");
            }
        });
    });
    //API InsertCabeceraNotaCredito InsertCuerpoNotaCredito nota de credito servicio descuento
    function insertaNotaCreditoServicioDescuento() {
        var cliente = $("#input-cliente").val();
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetPermisosCanalByCardCode", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cliente + '"}',
            dataType: "json",
            success: function (data) {
                let canal = data.Context[0].Canal;
                let email = data.Context[0].Email;
                $('#loading-nc').show();
                let itemcodessd = document.getElementsByName('itemcode_sd[]');
                let cantidadessd = document.getElementsByName('cantidad_sd[]');
                let preciossd = document.getElementsByName('precio_sd[]');
                let descuentossd = document.getElementsByName('descuento_sd[]');
                let usuario = localStorage.getItem('UserEmail');
                let folio_origen = $("#input-factura-origen").val();
                let folio_destino = $("#input-factura-destino").val();
                let tipo_documento = $("#select-tipo-documento option:selected").val();
                let concepto_descuento = $("#select-concepto-descuento option:selected").val();
                let Cuenta = $("#input-factura-destino-cuenta-servicio-descuento-hidden").val();
                let comentario = $('#comentario').val();
                $.ajax({
                    contentType: "application/json; charset=utf-8",
                    url: '@Url.Action("InsertCabeceraNotaCredito", "Venta")',
                    async: false,
                    timeout: 3000,
                    type: 'POST',
                    data: '{"CardCode":"' + cliente + '","FolioOrigen":"' + folio_origen + '","FolioDestino":"' + folio_destino + '","TipoDocumento":"' + tipo_documento + '","Comentario":"' + comentario + '","Usuario":"' + usuario + '","ConceptoDescuento":"' + concepto_descuento + '","Canal":"' + canal + '","Email":"' + email + '"}',
                    dataType: "json",
                    success: function (data) {
                        let FolioNC = data.Context;
                        //(FolioNC);
                        if (data.Code == "-1" || concepto_descuento=='0') {
                            limpiaTodito();
                            $("#table-invoice-history").empty();
                            getHistoricoNotaCredito();
                            $('#loading-nc').hide();
                            swal("Favor de verificar la logistica del descuento.");
                        } else {
                            for (var i = 0; i < itemcodessd.length; i++) {
                                var itemcodesd = itemcodessd[i];
                                var cantidadsd = cantidadessd[i];
                                var preciosd = preciossd[i];
                                var descuentosd = descuentossd[i];
                                $.ajax({
                                    url: '@Url.Action("InsertCuerpoNotaCredito", "Venta")',
                                    async: false,
                                    timeout: 3000,
                                    method: "POST",
                                    data: { FolioNC: FolioNC,ItemCode: itemcodesd.value, Cantidad: cantidadsd.value, Precio: preciosd.value, Descuento: descuentosd.value, Cuenta: Cuenta },
                                    success: function (data) {
                                    }
                                });
                            }
                            //ENVIO DE EMAIL NOTA DE CREDITO SERVICIO DESCUENTO
                            /*
                            let ToEmail = 'rafael.massorivera@fussionweb.com';
                            let ToEmail2 = 'eduardo.masso@fussionweb.com';
                            let ToEmail3 = 'adrian_rivera@fussionweb.com';
                            let ToEmail4 = 'eli_alvarez@fussionweb.com';
                            */
                            let ToEmail = '@System.Configuration.ConfigurationManager.AppSettings["EmailSistemas"]';
                            let Fecha = moment(new Date()).format('YYYY-MM-DD');
                            let FromEmail = '@System.Configuration.ConfigurationManager.AppSettings["EmailSistemasFussion"]';

                            let SubjectEmail = "NOTA DE CREDITO SERVICIO (Diferencia en precio)";
                            let cliente_txt = $('#span-cliente').text();
                            if (tipo_documento == 'I') {
                                tipo_documento = 'Inventario';
                            } else if (tipo_documento == 'S') {
                                tipo_documento = 'Servicio';
                            }
                            var html_code = "<div>";
                            html_code += "<h4 style='font-family: arial, sans-serif;'>Cliente: " + cliente_txt + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Folio origen: " + folio_origen + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Folio destino: " + folio_destino + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Tipo: " + tipo_documento + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Comentario: " + comentario + "</h4>";
                            html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px 0px 15px 0px; padding: 0px 0px 15px 0px;'>Fecha: " + Fecha + "</h4>";
                            html_code += "<table style='font-family: arial, sans-serif; border-collapse: collapse; width: 100%;'>";
                            html_code += "<thead>";
                            html_code += "<tr style='text-align: center; padding: 8px; background-color: rgb(221, 75, 57); color:#FFFFFF'>";
                            html_code += "<th>SKU</th>";
                            html_code += "<th>Cantidad</th>";
                            html_code += "<th>Precio</th>";
                            html_code += "<th>Subtotal</th>";
                            html_code += "<th>Descuento</th>";
                            html_code += "<th>Descuento total</th>";
                            html_code += "</tr>";
                            html_code += "</thead>";
                            html_code += "<tbody style='border: 2px solid black; border-collapse: separate; border-spacing: 4px; text-align: center'>";
                            let precio_formateadossd = document.getElementsByName('pre-formatsd[]');
                            let subtotal_formateadossd = document.getElementsByName('sub-formatsd[]');
                            let descuento_formateadossd = document.getElementsByName('des-formatsd[]');
                            let total_descuento_formateadossd = document.getElementsByName('total-formatsd[]');
                            var totalsd = 0;
                            for (var i = 0; i < itemcodessd.length; i++) {
                                var itemcodesd = itemcodessd[i];
                                var cantidadsd = cantidadessd[i];
                                var preciosd = preciossd[i];
                                var descuentosd = descuentossd[i];
                                var precio_formateadosd = precio_formateadossd[i];
                                var subtotal_formateadosd = subtotal_formateadossd[i];
                                var descuento_formateadosd = descuento_formateadossd[i];
                                var total_descuento_formateadosd = total_descuento_formateadossd[i];
                                var subtotalsd = parseFloat(cantidadsd.value) * parseFloat(descuentosd.value);
                                var totalsd = totalsd + subtotalsd;
                                html_code += "<tr style='text-align: center'>";
                                html_code += "<td>" + itemcodesd.value + "</td>";
                                html_code += "<td>" + cantidadsd.value + "</td>";
                                html_code += "<td>" + precio_formateadosd.value + "</td>";
                                html_code += "<td>" + subtotal_formateadosd.value +"</td>";
                                html_code += "<td>" + descuento_formateadosd.value + "</td>";
                                html_code += "<td>" + total_descuento_formateadosd.value + "</td>";
                                html_code += "</tr>";
                            }
                            html_code += "</tbody>";
                            html_code += "</table>";
                            let totalxsd = totalsd;
                            let ivaxsd = totalsd * 0.16;
                            let con_ivaxsd = totalsd * 1.16;
                            html_code += "<h4 style='font-family: arial, sans-serif; text-align: right;'>SUBTOTAL SIN IVA: " + formatNumber.new(totalxsd.toFixed(2), "$ ") + "</h4>";
                            //html_code += "<h4 style='font-family: arial, sans-serif; text-align: right;'>IVA: " + formatNumber.new(ivaxsd.toFixed(2), "$ ") + "</h4>";
                            //html_code += "<h4 style='font-family: arial, sans-serif; text-align: right;'>TOTAL: " + formatNumber.new(con_ivaxsd.toFixed(2), "$ ") + "</h4>";
                            html_code += "</div>";
                            $.ajax({
                                contentType: "application/json; charset=utf-8",
                                url: '@Url.Action("EmailSendNotaCredito", "Venta")',
                                async: false,
                                timeout: 3000,
                                type: 'POST',
                                data: '{"ToEmail":"' + ToEmail + '","ToEmail2":"' + ToEmail + '","ToEmail3":"' + ToEmail + '","ToEmail4":"' + ToEmail + '","FromEmail":"' + FromEmail + '","SubjectEmail":"' + SubjectEmail + '","BodyEmail":"' + html_code + '","Cliente":"' + cliente_txt + '"}',
                                dataType: "json",
                                success: function (data) {

                                },
                                error: function () {
                                    alert("Error en la api: EmailListPrice");
                                }
                            });
                            $('#loading-nc').hide();
                        }
                    },
                    error: function () {
                        swal("Error en la api: InsertCabeceraNotaCredito");
                    }
                });
            },
            error: function () {
                swal("Error en la api: UsuarioFolio");
            }
        });


    }
                                               /*FIN NOTA DE CREDITO SERVICIO DESCUENTO*/
    /**/
    //API GetFacturaNC SERVICIO OTRO DESCUENTO
    function consultaNotaCreditoOrigenServicioOtroDescuento(cadena_completa_cliente, factura_origen){
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaNC", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cadena_completa_cliente + '","DocNum":"' + factura_origen + '"}',
            dataType: "json",
            success: function (data) {
                $('#tbody-factura-origen-servicio-descuento').empty();
                if (data != null && $.isArray(data.Context)) {
                    if (JSON.stringify(data.Context) == "[]") {
                        $("#div-custom-origen").removeClass("has-success").addClass("has-error");
                        $(".span-help-block-origen").text("Folio origen no disponible");
                        //alert("Verificar el folio destino sea correcto. No se encontraron referencias de la factura destino verificar datos ingresados");
                    }
                    else {
                        $("#div-custom-origen").removeClass("has-error").addClass("has-success");
                        $(".span-help-block-origen").text("Folio origen disponible");
                    }
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaNC");
            }
        });
    }


    function consultaFacturaDestinoServicioOtroDescuento(factura_destino, cadena_completa_cliente) {
        if (cadena_completa_cliente == 'C002863') {
            // cliente de retail
            // aplica C002863 cliente soriana
            factura_destino = $('#input-factura-origen').val();
        }


        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaDestino", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"DocNum":"' + factura_destino + '","CardCode":"' + cadena_completa_cliente + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null && $.isArray(data.Context)) {

                    if (JSON.stringify(data.Context) == "[]") {
                        $("#div-custom-destino").removeClass("has-success").addClass("has-error");
                        $(".span-help-block-monto-destino").text("Folio destino no disponible");
                        $(this).next(".span-help-block-monto-destino").css("display", "inline").fadeOut(1000);
                        alert("!Favor de validar el folio destino. ya que no se encuentraron referencias del folio destino¡");
                    } else {
                        // canal al que pertenece al cliente
                        let canal = data.Context[0].Canal;

                        // total de la factura
                        let monto_facturado = data.Context[0].MontoFactura;


                        // monoto de la factura origen
                        let monto_facturado_origen = data.Context[0].MontoFacturaOrigen;


                        let concepto_descuento = $("#select-concepto-descuento option:selected").val();

                        //console.log("concepto_descuento " + concepto_descuento +" concepto_descuento");


                        if (canal == 'RETAIL') {
                            //alert("Retail");

                            let factura_origen = $('#input-factura-origen').val(); // folio de la factura origen

                            $.ajax({
                                contentType: "application/json; charset=utf-8",
                                url: '@Url.Action("GetFacturaOrigenNCRetail", "Venta")',
                                async: false,
                                timeout: 3000,
                                type: 'POST',
                                data: '{"DocNum":"' + factura_origen + '","CardCode":"' + cadena_completa_cliente + '"}',
                                dataType: "json",
                                success: function (data) {

                                    if (data != null && $.isArray(data.Context)) {

                                        let MontoOrigen = data.Context[0].MontoFacturaOrigen;

                                        $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                        $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                        $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(MontoOrigen);
                                        $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(MontoOrigen.toFixed(2));
                                    }
                                    else {
                                        swal("El monto origen para Retail no esta disponible verificar datos.");
                                    }
                                },
                                error: function () {
                                    swal("Error en la api: GetFacturaOrigenNCRetail. ");
                                }
                            });
                        }
                        else {
                            if (concepto_descuento == 39 && canal == 'MAYOREO') { // pronto pago
                               //
                                let factura_origen = $('#input-factura-origen').val(); // folio de la factura origen
                                $.ajax({
                                    contentType: "application/json; charset=utf-8",
                                    url: '@Url.Action("GetFacturaOrigenNC", "Venta")',
                                    async: false,
                                    timeout: 3000,
                                    type: 'POST',
                                    data: '{"DocNum":"' + factura_origen + '","CardCode":"' + cadena_completa_cliente + '"}',
                                    dataType: "json",
                                    success: function (data) {
                                        if (data != null && $.isArray(data.Context)) {

                                            //console.log("Concepto Pronto pago  Monto origen");

                                            let MontoOrigen = data.Context[0].MontoFacturaOrigen;


                                            $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                            $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(MontoOrigen);
                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                        }
                                    },
                                    error: function () {
                                        alert("Error en la api: GetFacturaOrigenNC");
                                    }
                                });
                                //console.log("Concepto de pronto pago");
                                //$("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                //$(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                                //$("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado_origen);
                                //$("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                            }
                            // 91 para sie daniel
                            // 93 para sie productivo
                            else if (concepto_descuento == '@System.Configuration.ConfigurationManager.AppSettings["ValueGarantias2"]' && canal == 'MAYOREO') { // aplica para Descuento por NO Garantias

                                let factura_origen = $('#input-factura-origen').val(); // folio de la factura origen
                                $.ajax({
                                    contentType: "application/json; charset=utf-8",
                                    url: '@Url.Action("GetFacturaOrigenNC", "Venta")',
                                    async: false,
                                    timeout: 3000,
                                    type: 'POST',
                                    data: '{"DocNum":"' + factura_origen + '","CardCode":"' + cadena_completa_cliente + '"}',
                                    dataType: "json",
                                    success: function (data) {
                                        if (data != null && $.isArray(data.Context)) {

                                            //console.log("Concepto Pronto pago  Monto origen");

                                            let MontoOrigen = data.Context[0].MontoFacturaOrigen;

                                            $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                            $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(MontoOrigen);
                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                        }
                                    },
                                    error: function () {
                                        alert("Error en la api: GetFacturaOrigenNC");
                                    }
                                });

                            }
                            // valido
                            else {

                                $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(monto_facturado.toFixed(2), "$ "));
                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado);
                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado.toFixed(2), "$ "));
                            }
                        }

                        $.ajax({
                            contentType: "application/json; charset=utf-8",
                            url: '@Url.Action("GetPorcentajePorIdConceptoServicio", "Venta")',
                            async: false,
                            timeout: 3000,
                            type: 'POST',
                            data: '{"Sequence":"' + concepto_descuento + '"}',
                            dataType: "json",
                            success: function (data) {
                                if (data != null && $.isArray(data.Context)) {

                                    let monto_facturado_descuento = 0, monto_facturado_descuento_origen = 0;
                                    let Cuenta = data.Context[0].Cuenta;
                                    // porcentaje del descuento
                                    let Descuento = data.Context[0].Descuento;

                                    // monto final de la factura con pag
                                    monto_facturado_descuento = monto_facturado * Descuento;

                                    // monto de la factura origen
                                    monto_facturado_descuento_origen = monto_facturado_origen * Descuento;


                                    if (canal == 'RETAIL') {

                                        if (Descuento == 0 || Descuento == 0.00) {
                                            //console.log("Es pronto pago y Mayoreo");
                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                                            $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val(monto_facturado_origen);
                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado_origen);
                                        }
                                        else {
                                            // monto factura origen
                                            //Aqui es el cambio para el cliente C002036
                                            monto_facturado_descuento_origen = monto_facturado_origen * Descuento;

                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                                            $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val(monto_facturado_descuento_origen);
                                            $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado_origen);

                                        }
                                        $("#input-factura-destino-cuenta-servicio-otro-descuento-hidden").val(Cuenta);
                                    }
                                    else {
                                        if (Descuento == 0 || Descuento == 0.00) {
                                            //console.log("Descuento a 0 n es retail");

                                            if (concepto_descuento == 39 && canal == 'MAYOREO') {
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                                                $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val(monto_facturado_origen);
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado_origen);

                                            }
                                            // aplica sie daniel 91
                                            // sie productivo 93
                                            else if (concepto_descuento == '@System.Configuration.ConfigurationManager.AppSettings["ValueGarantias2"]' && canal == 'MAYOREO') {
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado);
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                                            }
                                            else {
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado.toFixed(2), "$ "));
                                                $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val(monto_facturado);
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado);

                                            }
                                            $("#input-factura-destino-cuenta-servicio-otro-descuento-hidden").val(Cuenta);
                                        }
                                        else {
                                            // el descuento es diferente a cero
                                            if (concepto_descuento == 39 && canal == 'MAYOREO') {
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado_origen);
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                                                $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val(monto_facturado_descuento_origen);

                                            }
                                            // aplica sie daniel 91
                                            // sie productivo 93
                                            else if (concepto_descuento == '@System.Configuration.ConfigurationManager.AppSettings["ValueGarantias2"]' && canal == 'MAYOREO') {

                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado_origen.toFixed(2), "$ "));
                                                $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val(monto_facturado_descuento_origen);
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado_origen);
                                            }
                                            else {
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val(formatNumber.new(monto_facturado.toFixed(2), "$ "));
                                                $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val(monto_facturado_descuento_origen);
                                                $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val(monto_facturado);

                                            }
                                            $("#input-factura-destino-cuenta-servicio-otro-descuento-hidden").val(Cuenta);

                                        }
                                    }

                                }
                            },
                            error: function () {
                                alert("Error en la api: GetPorcentajePorIdConceptoServicio");
                            }
                        });
                    }
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaDestino");
            }
        });
    }
    // VALIDA MONTO SOLICITADO CONTRA MONTO PERMITIDO NOTA DE CREDITO OTRO SERVICIO DESCUENTO
    function validarCantidadPermitidaNotaCreditoServicioOtroDescuento() {
        let monto_solicitado_nota_credito_otro_servicio_descuento_hidden = $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val();
        let monto_solicitado_nota_credito_otro_servicio_descuento = $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").val();
        let parse_monto_solicitado_hidden = parseFloat(monto_solicitado_nota_credito_otro_servicio_descuento_hidden);
        let parse_monto_solicitado = parseFloat(monto_solicitado_nota_credito_otro_servicio_descuento);
        //if (parse_monto_solicitado > (parse_monto_solicitado_hidden / 1.16) + 300) {
        //    swal("El monto solicitado no puede superar al permitido");
        //    $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").val(parse_monto_solicitado_hidden).slice(0, 3);
        //}
    }

    //API GetFacturaNCValidacionLimiteDinero SERVICIO OTRO DESCUENTO
    function getFacturaNCValidacionLimiteDineroServicioOtroDescuento(cadena_completa_cliente,factura_destino,factura_origen) {
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaNCValidacionLimiteDinero", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cadena_completa_cliente + '","FolioDestino":"' + factura_destino + '","FolioOrigen":"' + factura_origen + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null && $.isArray(data.Context)) {

                    let Origen = $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val();
                    let Subtotal = data.Context[0].Subtotal;

                    let Canal = data.Context[0].Canal;

                    let Resta = Origen - Subtotal;

                    if (Canal == 'RETAIL') {
                        Resta = Origen;
                    }

                    $("#input-factura-destino-monto-solicitado-servicio-otro-descuento-hidden").val(Resta);
                    $(".span-help-block-destino-monto-ocupado").text("Monto ocupado: " + formatNumber.new(Subtotal.toFixed(2), "$ "));
                    $(".span-help-block-destino-monto-disponible").text("Monto disponible: " + formatNumber.new(Resta, "$ "));
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaNCValidacionLimiteDinero");
            }
        });
    }
    //aA SOLICITUD DE NOTA DE CREDITO TIPO SERVICIO OTRO DESCUENTO --ojo (Pronto pago)
    document.querySelector('#boton-solicitar-nota-credito-otro-servicio-descuento').addEventListener('click', function (e) {
        e.preventDefault();

        let montoDespuesDescuento = $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val();
        let montoIntroducido = $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").val();
        let montoDespuesDescuentoSinIVA = montoDespuesDescuento / 1.16;

        if (montoIntroducido < (montoDespuesDescuentoSinIVA - 300)) {
            swal("El monto solicitado no puede ser menor a lo permitido");
            $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").val(montoDespuesDescuentoSinIVA).slice(0, 3);
            return;
        }

        swal({
            title: "¿Solicitar nota de credito?",
            text: "Se mandará solicitud de la nota de crédito (otros decuentos).",
            icon: "warning",
            buttons: [
                'No, cancelar esta operación',
                'Si, estoy seguro'
            ],
            dangerMode: true,
        }).then(function (isConfirm) {
            if (isConfirm) {
                swal({
                    title: 'Operación confirmada',
                    text: 'Los datos se mandaron correctamente',
                    icon: 'success'
                }).then(function () {
                    var cliente = $("#input-cliente").val();
                    $.ajax({
                        contentType: "application/json; charset=utf-8",
                        url: '@Url.Action("GetPermisosCanalByCardCode", "Venta")',
                        async: false,
                        timeout: 3000,
                        type: 'POST',
                        data: '{"CardCode":"' + cliente + '"}',
                        dataType: "json",
                        success: function (data) {

                            $('#loading-nc').show();
                            let canal = data.Context[0].Canal;
                            let email = data.Context[0].Email;


                            let folio_origen = $("#input-factura-origen").val();
                            let folio_destino = $("#input-factura-destino").val();
                            let tipo_documento = $("#select-tipo-documento option:selected").val();
                            let concepto_descuento = $("#select-concepto-descuento option:selected").val();
                            let concepto_descuento_text = $("#select-concepto-descuento option:selected").text();
                            let Cuenta =  $("#input-factura-destino-cuenta-servicio-otro-descuento-hidden").val();
                            let comentario = $('#comentario').val();

                            // anterior
                            let monto_permitido_osd = $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val();

                           // alert("Saldo ORIGINAL: " + monto_permitido_osd);

                            //let monto_solicitado_osd = $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val();
                            let monto_solicitado_osd = $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").val();
                            let usuario = localStorage.getItem("UserEmail");
                            //alert("Monto aprobado: " + monto_solicitado_osd);


                            $.ajax({
                                contentType: "application/json; charset=utf-8",
                                url: '@Url.Action("InsertCabeceraNotaCredito", "Venta")',
                                async: false,
                                timeout: 3000,
                                type: 'POST',
                                data: '{"CardCode":"' + cliente + '","FolioOrigen":"' + folio_origen + '","FolioDestino":"' + folio_destino + '","TipoDocumento":"' + tipo_documento + '","Comentario":"' + comentario + '","Usuario":"' + usuario + '","ConceptoDescuento":"' + concepto_descuento + '","Canal":"' + canal + '","Email":"' + email + '"}',
                                dataType: "json",
                                success: function (data) {
                                    alert("entro y regreso datos de cabecera"+ JSON.stringify(data))
                                    if (data.Code == "-1" || concepto_descuento == '0') {
                                        limpiaTodito();
                                        $("#table-invoice-history").empty();
                                        getHistoricoNotaCredito();
                                        $('#loading-nc').hide();
                                        swal("Favor de verificar la logistica del descuento");
                                    } else {
                                        alert("Entro para agregar cuerpo")
                                        let FolioNC = data.Context;

                                        let DataArray = '{"FolioNC":"' + FolioNC + '","ItemCode":"' + concepto_descuento_text + '","Cantidad":"' + 1 + '","Precio":"' + monto_permitido_osd + '","Descuento":"' + monto_solicitado_osd + '","Cuenta":"' + Cuenta + '"}';
                                        //alert(JSON.stringify(DataArray));
                                        $.ajax({
                                            contentType: "application/json; charset=utf-8",
                                            url: '@Url.Action("InsertCuerpoNotaCredito", "Venta")',
                                            async: false,
                                            timeout: 3000,
                                            method: "POST",
                                            data: DataArray,
                                            //data: { FolioNC: FolioNC ,ItemCode: concepto_descuento_text, Cantidad: 1, Precio: monto_permitido_osd, Descuento: monto_solicitado_osd, Cuenta: Cuenta },
                                            success: function (data) {
                                                alert("regreso de guardar el cuerpo de la nc"+ JSON.stringify(data));
                                            },
                                            error: function () {
                                                alert("Error en la api: InsertCuerpoNotaCredito");
                                                e.stopPropagation();
                                            }
                                        });
                                        //ENVIO DE EMAIL NOTA DE CREDITO OTRO SERVICIO DESCUENTO

                                        let ToEmail = '@System.Configuration.ConfigurationManager.AppSettings["EmailSistemas"]';

                                        let Fecha = moment(new Date()).format('YYYY-MM-DD');
                                        let FromEmail = localStorage.getItem('UserEmail');
                                        let seleccion = $("#select-concepto-descuento option:selected").text()

                                        let SubjectEmail = "NOTA DE CREDITO SERVICIO (" + seleccion + ")";
                                        let cliente_txt = $('#span-cliente').text();

                                        // aqui debe de validar si la nota de credito
                                        // inventario
                                        if (tipo_documento == 'I') {
                                            tipo_documento = 'Inventario';
                                        }
                                        // servicio
                                        else if (tipo_documento == 'S') {
                                            tipo_documento = 'Servicio';
                                        }

                                        let monto_permitido_osdf = $("#input-factura-destino-monto-permitido-servicio-otro-descuento").val();
                                        let monto_solicitado_osdf = $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").val();
                                        let monto_solicitado_osdx = formatNumber.new(monto_solicitado_osdf, "$ ");

                                        var html_code = "<div>";
                                        html_code += "<h4 style='font-family: arial, sans-serif;'>FOLIO SIE: " + FolioNC + "</h4>";
                                        html_code += "<h4 style='font-family: arial, sans-serif;'>Cliente: " + cliente_txt + "</h4>";
                                        html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Folio origen: " + folio_origen + "</h4>";
                                        html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Folio destino: " + folio_destino + "</h4>";
                                        html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Tipo: " + tipo_documento + "</h4>";
                                        html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px; padding: 0px;'>Comentario: " + comentario + "</h4>";
                                        html_code += "<h5 style='font-family: arial, sans-serif; margin: 0px 0px 15px 0px; padding: 0px 0px 15px 0px;'>Fecha: " + Fecha + "</h4>";
                                        html_code += "<table style='font-family: arial, sans-serif; border-collapse: collapse; width: 100%;'>";
                                        html_code += "<thead>";
                                        html_code += "<tr style='text-align: center; padding: 8px; background-color: rgb(221, 75, 57); color:#FFFFFF'>";
                                        html_code += "<th>SKU</th>";
                                        html_code += "<th>Cantidad</th>";
                                        html_code += "<th>Precio</th>";
                                        html_code += "<th>Descuento</th>";
                                        html_code += "</tr>";
                                        html_code += "</thead>";
                                        html_code += "<tbody style='border: 2px solid black; border-collapse: separate; border-spacing: 4px; text-align: center'>";
                                        html_code += "<tr style='text-align: center'>";
                                        html_code += "<td>" + concepto_descuento_text + "</td>";
                                        html_code += "<td>1</td>";
                                        html_code += "<td>" + monto_permitido_osdf+"</td>";
                                        html_code += "<td>" + monto_solicitado_osdx+"</td>";
                                        html_code += "</tr>";
                                        html_code += "</tbody>";
                                        html_code += "</table>";
                                        let minus_iva = 0.16;
                                        let iva_detail = parseFloat(monto_solicitado_osd) * parseFloat(minus_iva);
                                        let subtotal_detail = parseFloat(monto_solicitado_osd) - parseFloat(iva_detail);
                                        let totalizado = parseFloat(monto_solicitado_osd);
                                        html_code += "<h4 style='font-family: arial, sans-serif; text-align: right;'>SUBTOTAL SIN IVA: " + formatNumber.new(totalizado, "$ ") + "</h4>";
                                        html_code += "<h4 style='font-family: arial, sans-serif;'>Registrado por: " + usuario + "</h4>";
                                        html_code += "</div'>";
                                        $.ajax({
                                            contentType: "application/json; charset=utf-8",
                                            url: '@Url.Action("EmailSendNotaCredito", "Venta")',
                                            async: false,
                                            timeout: 3000,
                                            type: 'POST',
                                            data: '{"ToEmail":"' + ToEmail + '","ToEmail2":"' + ToEmail + '","ToEmail3":"' + ToEmail + '","ToEmail4":"' + ToEmail + '","FromEmail":"' + FromEmail + '","SubjectEmail":"' + SubjectEmail + '","BodyEmail":"' + html_code + '", "Cliente":"' + cliente + '"}',
                                            dataType: "json",
                                            success: function (data) {
                                                limpiaTodito();
                                                $("#table-invoice-history").empty();
                                                getHistoricoNotaCredito();
                                                $('#loading-nc').hide();
                                            },
                                            error: function () {
                                                alert("Error en la api: EmailSendNotaCredito");
                                            }
                                        });
                                    }
                                },
                                error: function () {
                                    swal("Error en la api: InsertCabeceraNotaCredito");
                                }
                            });
                        },
                        error: function () {
                            alert("Error en la api: GetPermisosCanalByCardCode");
                        }
                    });
                });
            } else {
                swal("Cancelado", "Operación cancelada", "error");
            }
        });

    });
                                                    /*FIN NOTA DE CREDITO SERVICIO OTRO DESCUENTO*/
    /**/

    //MODAL NOTAS DE CREDITO
    function getHistoricoNotaCreditoDetail(FolioNotaCredito) {
        resetFormsGestion();

        $(".main-timeline-section").show();
        $("#estatus-rechazado").text("");

        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetNotaCreditoItem", "NotaCredito")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"NotaCredito":"' + FolioNotaCredito + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null) {

                    let FechaFormato = moment(data.Context.Fecha).format("YYYY-MM-DD HH:mm:ss");
                    let ConceptoDescuento = data.Context.ConceptoDescuento;

                    let TypeDoc = data.Context.TipoDocumento;
                    if (TypeDoc == 'S') {
                        TypeDoc = 'Servicio';
                    }
                    if (TypeDoc == 'I') {
                        TypeDoc = 'Inventario';
                    }
                    let canal = data.Context.Canal;
                    //if (ConceptoDescuento != 38 && ConceptoDescuento != 39) {
                    //    $(".line-time-change-text").text("");
                    //    $(".line-time-change").text("");
                    //    $(".gestor-timeline-change").css("visibility", "hidden");
                    //}
                    let Total = 0;
                    $("#table-invoice-detail").empty();

                    $.each(data.Context.Items, function (index, value) {
                        let SubTotalDetail = 0;
                        let Descuento = 0;
                        switch (data.Context.TipoDocumento) {
                            case 'I':
                                SubTotalDetail = value.Cantidad * value.Precio;
                                Descuento = SubTotalDetail;
                                break;
                            case 'S':
                                SubTotalDetail = value.Cantidad * value.Descuento;
                                Descuento = value.Descuento;
                                break;
                            default:
                        }
                        if (ConceptoDescuento == 37) {
                            SubTotalDetail = value.Cantidad * value.Descuento;
                            Descuento = value.Descuento;
                        }
                        Total = Total + SubTotalDetail;

                        $("#table-invoice-detail").append(
                            "<tr>\
                                <td>" + value.ItemCode + "</td>\
                                <td align='center'>" + value.Cantidad + "</td >\
                                <td align='center'>" + formatNumber.new(value.Precio.toFixed(2), "$ ") + "</td>\
                                <td style='display:none'><input type='text' value='" + SubTotalDetail + "'/></td>\
                                <td align='center'>" + formatNumber.new(value.Descuento.toFixed(2), "$ ") + "</td>\
                                <td align='center' style='display:none'><input type='text' class='sub-total-detail' value='" + SubTotalDetail + "'/></td>\
                                <td align='center'>" + formatNumber.new(SubTotalDetail.toFixed(2), "$ ") + "</td>\
                            </tr>");

                    });
                    $('#total-detail').text(formatNumber.new(Total.toFixed(2), "$ "));

                    let Estatus = data.Context.Valor;
                    switch (Estatus) {
                        case 0:
                            $("#estatus-gerencia").addClass('meta-date');
                            $("#estatus-almacen").addClass('meta-date');
                            $("#estatus-credito").addClass('meta-date');
                            $("#estatus-direccion").addClass('meta-date');
                            $('.timeline-end').addClass('meta-date')
                            break;
                        case 1:
                            $("#estatus-gerencia").addClass('meta-date-gestion');
                            //$("#estatus-almacen").addClass('meta-date');
                            //$("#estatus-credito").addClass('meta-date');
                            //$("#estatus-direccion").addClass('meta-date');
                            $('.timeline-end').addClass('meta-date')
                            break;
                        case 2:
                            $("#estatus-gerencia").addClass('meta-date-gestion');
                            $("#estatus-almacen").addClass('meta-date-gestion');
                            $("#estatus-credito").addClass('meta-date-gestion');
                            //$("#estatus-direccion").addClass('meta-date');
                            $('.timeline-end').addClass('meta-date')
                            break;
                        case 3:
                            $("#estatus-gerencia").addClass('meta-date-gestion');
                            $("#estatus-almacen").addClass('meta-date-gestion');
                            $("#estatus-credito").addClass('meta-date-gestion');
                            $("#estatus-direccion").addClass('meta-date-gestion');
                            $('.timeline-end').addClass('meta-date')
                            break;
                        case 4:
                            $("#estatus-gerencia").addClass('meta-date-gestion');
                            $("#estatus-almacen").addClass('meta-date-gestion');
                            $("#estatus-credito").addClass('meta-date-gestion');
                            $("#estatus-direccion").addClass('meta-date-gestion');
                            $('.timeline-end').addClass('meta-date-gestion')
                            break;
                        case 5:
                            $(".main-timeline-section").hide();
                            $("#estatus-rechazado").text("NOTA DE CREDITO RECHAZADA");
                            break;
                    }

                    if (ConceptoDescuento == 37) { // diferencia en precio 100%

                        $("#estatus-gerencia").addClass('meta-date-gestion');
                        $("#estatus-direccion").addClass('meta-date-gestion');

                        $('#div-gestion-gerencia').show();
                        $('#estatus-gerencia').show();

                    }

                    else if (ConceptoDescuento == 38) { // inventario
                        $('#estatus-gerencia').show();
                        $('#div-gestion-gerencia').show();

                        $('#estatus-almacen').show();
                        $('#div-gestion-almacen').show();
                    }

                    else if (ConceptoDescuento == 39) { // pronto pago
                        $('#div-gestion-credito').show();
                        $('#estatus-credito').show();

                    }
                    else if (canal == 'RETAIL' && data.Context.TipoDocumento != 'I' && ConceptoDescuento != 39 && ConceptoDescuento != 38 && ConceptoDescuento != 37) {
                        $('#div-gestion-credito').show();
                        $('#div-gestion-gerencia').show();
                        $('#estatus-gerencia').show();
                        $('#estatus-credito').show();

                        $('#div-gestion-almacen').hide();
                        $('#estatus-almacen').hide();
                    }
                    else {
                        $('#estatus-gerencia').show();
                        $('#div-gestion-gerencia').show();
                    }

                    $("#set-Sequence").text("FOLIO SIE: " + data.Context.Identifier);
                    $("#set-Cliente").text("CLIENTE: " + data.Context.ClienteName);
                    $("#set-FolioOrigen").text("FOLIO ORIGEN: " + data.Context.FolioOrigen);
                    $("#set-FolioDestino").text("FOLIO DESTINO: " + data.Context.FolioDestino);
                    $("#set-Comentario").text("COMENTARIO: " + data.Context.Comentario);
                    $("#set-Usuario").text("USUARIO: " + data.Context.Usuario);
                    $("#set-TipoDocumento").text("TIPO DOCUMENTO: " + TypeDoc);
                    $("#set-Fecha").text("FECHA: " + FechaFormato);

                }
            }, error: function (data) {
                swal("Error en la api: GetNotaCreditoItem: " + data);
            }
        });
    }

    function resetFormsGestion() {
        $('#div-gestion-almacen').hide();
        $('#div-gestion-gerencia').hide();
        $('#div-gestion-credito').hide();

        $('#estatus-gerencia').hide();
        $('#estatus-almacen').hide();
        $('#estatus-credito').hide();

        $("#estatus-gerencia").removeClass("meta-date-gestion").addClass('meta-date');
        $("#estatus-almacen").removeClass("meta-date-gestion").addClass('meta-date');
        $("#estatus-credito").removeClass("meta-date-gestion").addClass('meta-date');
        $("#estatus-direccion").removeClass("meta-date-gestion").addClass('meta-date');

        $('.timeline-end').removeClass("meta-date-gestion").addClass('meta-date');

    }
    //CALCULAR SUBTOTAL NOTA DE CREDITO INVENTARIO
    function calculaMotosNotaCreditoInventarioModal() {
        var sum = 0;
        $(".sub-total-detail").each(function () {
            let sumado = this.value;
            if (!isNaN(sumado) && sumado.length != 0) {
                sum += parseFloat(sumado);
            }
        });
        let subtotal_detail = sum;
        $('#total-detail').text("Total sin IVA: " + formatNumber.new(total_detail.toFixed(2), "$ "));
    };
    //PASA SEQUENCE AL MODAL NOTA DE CREDITO INVENTARIO
    $('#modal-nota-credito-inventario').on('show.bs.modal', function (e) {
        var id = $(e.relatedTarget).data().id;
        $(e.currentTarget).find('#sequence').val(id);
    });

    //API GetFacturaDestino  aplica solo para inventario
    function consultaFacturaDestino(factura_destino, cadena_completa_cliente, concepto_descuento) {

        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaDestino", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"DocNum":"' + factura_destino + '","CardCode":"' + cadena_completa_cliente + '"}',
            dataType: "json",
            success: function (data) {
                if (data != null && $.isArray(data.Context)) {

                    if (JSON.stringify(data.Context) == "[]") {
                        $("#div-custom-destino").removeClass("has-success").addClass("has-error");
                        $(".span-help-block-monto-destino").text("Folio destino no disponible");
                        $(this).next(".span-help-block-monto-destino").css("display", "inline").fadeOut(1000);
                        alert("!Favor de validar el folio destino. ya que no se encuentraron referencias del folio destino¡");
                    } else {
                        let canal = data.Context[0].Canal;

                        let monto_facturado = data.Context[0].MontoFactura;

                        //validar mayoreo y pronto pago  descuen monto origen
                        // valor (1,2,3)
                        // aplicar para descuentos de no garantia

                        if (canal == 'RETAIL') { // solo aplica para retail
                            // se obtiene el monto de la factura origen


                            let factura_origen = $('#input-factura-origen').val(); // folio de la factura origen

                            $.ajax({
                                contentType: "application/json; charset=utf-8",
                                url: '@Url.Action("GetFacturaOrigenNC", "Venta")',
                                async: false,
                                timeout: 3000,
                                type: 'POST',
                                data: '{"DocNum":"' + factura_origen + '","CardCode":"' + cadena_completa_cliente + '"}',
                                dataType: "json",
                                success: function (data) {
                                    if (data != null && $.isArray(data.Context)) {
                                        let MontoOrigen = data.Context[0].MontoFacturaOrigen;
                                        $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                        $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                        $("#input-factura-destino-monto-permitido").val(MontoOrigen);
                                    }
                                },
                                error: function () {
                                    alert("Error en la api: GetFacturaOrigenNC");
                                }
                            });

                        }
                        else {
                            if (canal == 'MAYOREO' && concepto_descuento == 39) { // pronto pago
                                let factura_origen = $('#input-factura-origen').val(); // folio de la factura origen

                                $.ajax({
                                    contentType: "application/json; charset=utf-8",
                                    url: '@Url.Action("GetFacturaOrigenNC", "Venta")',
                                    async: false,
                                    timeout: 3000,
                                    type: 'POST',
                                    data: '{"DocNum":"' + factura_origen + '","CardCode":"' + cadena_completa_cliente + '"}',
                                    dataType: "json",
                                    success: function (data) {
                                        if (data != null && $.isArray(data.Context)) {
                                            let MontoOrigen = data.Context[0].MontoFacturaOrigen;
                                            $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                            $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                            $("#input-factura-destino-monto-permitido").val(MontoOrigen);
                                        }
                                    },
                                    error: function () {
                                        alert("Error en la api: GetFacturaOrigenNC");
                                    }
                                });
                            }
                            else if (canal == 'MAYOREO' && concepto_descuento == '@System.Configuration.ConfigurationManager.AppSettings["ValueGarantias2"]') { // no garantias
                                let factura_origen = $('#input-factura-origen').val(); // folio de la factura origen

                                $.ajax({
                                    contentType: "application/json; charset=utf-8",
                                    url: '@Url.Action("GetFacturaOrigenNC", "Venta")',
                                    async: false,
                                    timeout: 3000,
                                    type: 'POST',
                                    data: '{"DocNum":"' + factura_origen + '","CardCode":"' + cadena_completa_cliente + '"}',
                                    dataType: "json",
                                    success: function (data) {
                                        if (data != null && $.isArray(data.Context)) {
                                            let MontoOrigen = data.Context[0].MontoFacturaOrigen;
                                            $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                            $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(MontoOrigen.toFixed(2), "$ "));
                                            $("#input-factura-destino-monto-permitido").val(MontoOrigen);
                                        }
                                    },
                                    error: function () {
                                        alert("Error en la api: GetFacturaOrigenNC");
                                    }
                                });
                            }
                            else {
                                $("#div-custom-destino").removeClass("has-error").addClass("has-success");
                                $(".span-help-block-monto-destino").text("Monto destino con iva: " + formatNumber.new(monto_facturado.toFixed(2), "$ "));
                                $("#input-factura-destino-monto-permitido").val(monto_facturado);
                            }
                        }
                    }
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaDestino");
            }
        });
    }
    //API GetFacturaNC
    function consultaNotaCreditoOrigen(cadena_completa_cliente,factura_origen) {
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetFacturaNC", "Venta")',
            async: true,
            //timeout: 3000,
            type: 'POST',
            data: '{"CardCode":"' + cadena_completa_cliente + '","DocNum":"' + factura_origen + '"}',
            dataType: "json",
            success: function (data) {
                $('#tbody-factura-origen-inventario').empty();
                if (data != null && $.isArray(data.Context)) {
                    if (JSON.stringify(data.Context) == "[]") {
                        $("#div-custom-origen").removeClass("has-success").addClass("has-error");
                        $(".span-help-block-origen").text("Folio origen no disponible");
                        //alert("Verificar el folio destino sea correcto. No se encontraron referencias de la factura destino verificar datos ingresados");
                    }
                    else {


                        $('#loading').show();
                        $("#div-custom-origen").removeClass("has-error").addClass("has-success");
                        $(".span-help-block-origen").text("Folio origen disponible");
                        $.each(data.Context, function (index, value) {
                            $("#tbody-factura-origen-inventario").append(
                                "<tr id='tr-invoice-" + index + "'>\
                                    <td class='color-sku' ondblclick='return clonarTablaNotaDeCreditoInventario(\"" + value.Codigo + "\",\"" + value.Precio + "\",\"" + index + "\",\"" + value.AcctCode + "\",\"" + value.Cantidad + "\")'>" + value.Codigo + "</td>\
                                    <td><input type='text 'id='cantidad-inventario-"+ index + "' min='1' value='" + value.Cantidad + "' onkeyup='validarCantidadPermitidaNotaCreditoInventario(" + index + ")' onkeypress='return isNumber(event)' style='width: 70px'/></td>\
                                    <td><input type='text' id='cantidad-inventario-default-"+ index + "' value='" + value.Cantidad + "' style='width: 60px' disabled></td >\
                                    <td id='td-concepto-descuento-fijo-"+ index + "'>" + formatNumber.new(value.Precio.toFixed(3), "$ ") + "</td>\
                                    <td id='td-concepto-descuento-"+ index + "'><input type='text' id='input-concepto-" + index +"' style='width: 70px'/></td>\
                                    <td  style='display:none'><input type='text' id='input-cuenta-contable-inventario-"+ index + "' value='" + value.AcctCode +"'/></td>\
                                    </tr>");
                            $("#td-concepto-descuento-" + index).hide();
                        });
                        $("#th-concepto-descuento").hide();
                        $('#loading').hide();
                    }
                }
            },
            error: function () {
                alert("Error en la api: GetFacturaNC");
            }
        });
    }
    //API GetConceptosServicios
    function getConceptosServicios() {
        let cadena_completa_cliente = $('#input-cliente').val();
        $.post('@Url.Action("GetConceptosServicios", "Venta")', {
            Clientes: cadena_completa_cliente
        },
        function (data) {
            if (data != null && $.isArray(data.Context)) {
                $("#select-concepto-descuento").empty();
                let selectConceptosServicios = $("#select-concepto-descuento");
                selectConceptosServicios.append("<option value='0' selected='selected'>Seleccionar...</option>");
                $.each(data.Context, function (index, value) {

                    var DescuentoFormato = value.Descuento * 100;
                    selectConceptosServicios.append(
                        "<option class='option-concepto-" + index + "' value=" + value.Sequence + ">" + value.Nombre + " --- " + DescuentoFormato.toFixed(3)+ '%' + "</option>")
                });


            } else {
                $.alert("Error en la api: ListaTiendas");
            }
        });
    }

    //BUSCA CLIENTE 1
    var options = {
        adjustWidth: true,
        url: function (phrase) {
            return "@Url.Action("FindCliente", "Venta")";
        },
        getValue: function (element) {
            getConceptosServicios();
            esconderTodasLasTablas();
            return element.Coincidencia;
        },
        list: {
            onSelectItemEvent: function () {
                var Codigo = $("#input-cliente").getSelectedItemData().Codigo;
                $("#input-cliente").val(Codigo).trigger("change");
                var Codigo1 = $("#input-cliente").getSelectedItemData().Coincidencia;
                $("#span-cliente").text(Codigo1);
            },
            onHideListEvent: function () {
                $("#input-cliente").val(Codigo).trigger("change");
            }
        },
        ajaxSettings: {
            dataType: "json",
            method: "POST",
            data: { dataType: "json" }
        },
        preparePostData: function (data) {
            data.phrase = $("#input-cliente").val();
            return data;
        },
        requestDelay: 400
    };
    $("#input-cliente").easyAutocomplete(options);

    //VALIDA DECIMAL
    $('.number-only').keyup(function (e) {
        if (this.value != '-')
            while (isNaN(this.value))
                this.value = this.value.split('').reverse().join('').replace(/[\D]/i, '')
                    .split('').reverse().join('');
    }).on("cut copy paste", function (e) {
        e.preventDefault();
        });


    //VALIDA INT
    function isNumber(evt) {
        evt = (evt) ? evt : window.event;
        var charCode = (evt.which) ? evt.which : evt.keyCode;
        if (charCode > 31 && (charCode < 48 || charCode > 57)) {
            return false;
        }
        return true;
    }

    function genScreenshot() {
        html2canvas(document.getElementById('modal-nota-credito-inventario'), {
            onrendered: function (canvas) {
                $('#box1').html("");
                $('#box1').append(canvas);

                if (navigator.userAgent.indexOf("MSIE ") > 0 ||
                    navigator.userAgent.match(/Trident.*rv\:11\./)) {
                    var blob = canvas.msToBlob();
                    window.navigator.msSaveBlob(blob, 'Test file.png');
                }
                else {
                    $('#test').attr('href', canvas.toDataURL("image/png"));
                    $('#test').attr('download', 'Test file.png');
                    $('#test')[0].click();
                }
            }
        });
    }

    function verComentarios(SequenceForeignKey) {
        $('#Identifier_Comentario').val('');
        $('#text-comentario').val('');
        $('#Identifier_Comentario').val(SequenceForeignKey);
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetComentarioNotaCredito", "Venta")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"SequenceForeignKey":"' + SequenceForeignKey + '"}',
            dataType: "json",
        }).done(function (data) {
            if (data != null && $.isArray(data.Context)) {
                if (JSON.stringify(data.Context) == "[]") {
                    $('#set-comentario').text('Sin comentarios');
                } else {
                    $('#set-comentario').text('');
                    $("#tbody-comentarios").empty();
                    $.each(data.Context, function (index, value) {
                        if (value.Comentario == "") {
                            value.Comentario = 'Sin comentario';
                        }
                        if (value.Departamento == 1) {
                            value.Departamento = 'Gerencia';
                        }
                        if (value.Departamento == 2) {
                            value.Departamento = 'Almacén';
                        }
                        if (value.Departamento == 3) {
                            value.Departamento = 'Dirección';
                        }
                        if (value.Departamento == 5) {
                            value.Departamento = 'Crédito y Cobranza';
                        }
                        if (value.Departamento == 6) {
                            value.Departamento = 'Sistemas';
                        }
                        if (value.Departamento == 10) {
                            value.Departamento = 'Ventas';
                        }
                        if (value.Departamento == 20) {
                            value.Departamento = 'Por usuario';
                        }

                        $("#tbody-comentarios").append(
                            "<tr>\
                            <td>" + value.Departamento + "</td>\
                            <td>" + value.Usuario + "</td >\
                            <td>" + value.Comentario + "</td >\
                        </tr>");
                    });
                }

            }
        }).always(function (data) {
        }).fail(function (data) {
        });
    }

    //BUSCA CLIENTE
    var options = {
        adjustWidth: true,
        url: function (phrase) {
            return "@Url.Action("FindCliente", "Venta")";
        },
        getValue: function (element) {

           // $("#span-cliente").text(element.Coincidencia);
            return element.Coincidencia;
        },
        list: {
            onSelectItemEvent: function () {
                var Codigo = $("#input-cliente").getSelectedItemData().Codigo;
                $("#input-cliente").val(Codigo).trigger("change");

                var Codigo1 = $("#input-cliente").getSelectedItemData().Coincidencia;
                $("#span-cliente").text(Codigo1);

            },
            onHideListEvent: function () {
                $("#input-cliente").val(Codigo).trigger("change");
            }
        },
        ajaxSettings: {
            dataType: "json",
            method: "POST",
            data: { dataType: "json" }
        },
        preparePostData: function (data) {
            data.phrase = $("#input-cliente").val();
            return data;
        },
        requestDelay: 400
    };

    $("#input-cliente").easyAutocomplete(options);

    $("#input-cliente").click(function () {
        //getConceptosServicios();
        limpiaToditoMenosCliente();
    });



    //FORMATO DINERO
    var formatNumber = {
        separador: ",",
        sepDecimal: '.',
        formatear: function (num) {
            num += '';
            var splitStr = num.split('.');
            var splitLeft = splitStr[0];
            var splitRight = splitStr.length > 1 ? this.sepDecimal + splitStr[1] : '';
            var regx = /(\d+)(\d{3})/;
            while (regx.test(splitLeft)) {
                splitLeft = splitLeft.replace(regx, '$1' + this.separador + '$2');
            }
            return this.simbol + splitLeft + splitRight;
        },
        new: function (num, simbol) {
            this.simbol = simbol || '';
            return this.formatear(num);
        }
    }

    //LIMPIA TODO CONTROLES INVENTARIO
    document.querySelector("#boton-cancelar-nota-credito-inventario").addEventListener("click", function (e) {
        e.preventDefault();
        //INPUTS FORMULARIO
        //$("#input-cliente").val("");
        $("#boton-solicitar-nota-credito-inventario").hide();
        $("#input-factura-origen").val("");
        $("#input-factura-destino").val("");
        $("#comentario").val("");
        $("#input-factura-destino-monto-permitido").val("");
        $("#input-factura-destino-monto-solicitado").val("");
        $("#input-factura-destino-monto-disponible").val("");
        //SPAN LABELS
        $(".span-help-block-monto-destino").text("");
        $(".span-help-block-destino-monto-disponible").text("");
        $(".span-help-block-destino-monto-ocupado").text("");
        $(".span-help-block-origen").text("");
        //DIVS MENSAJES COLOR VERDE O ROJO
        $("#div-custom-origen").removeClass("has-success");
        $("#div-custom-origen").removeClass("has-error");
        $("#div-custom-destino").removeClass("has-success");
        $("#div-custom-destino").removeClass("has-error");
        //RESET TABLAS
        $("#tabla-factura-origen-inventario").hide();
        $("#tabla-nota-credito-solicitada-inventario").hide();
        $("#tbody-factura-origen-inventario").html("");
        $("#tbody-nota-credito-solicitada-inventario").html("");
        //RESET SPAN
        $("#iva-inventario").text("");
        $("#subtotal-inventario").text("");
        $("#total-inventario").text("");
        //RESETEA SELECTS FORMULARIO
        reseteaSelectsFormulario();
    });

    //LIMPIA TODO CONTROLES SERVICIO DIFERENCIA EN PRECIO
    document.querySelector("#boton-cancelar-nota-credito-servicio-descuento").addEventListener("click", function (e) {
        e.preventDefault();
        //INPUTS FORMULARIO
        //$("#input-cliente").val("");
        $("#boton-solicitar-nota-credito-servicio-descuento").hide();
        $("#input-factura-destino").val("");
        $("#input-factura-origen").val("");
        $("#comentario").val("");
        $("#input-factura-destino-monto-permitido-servicio-descuento").val("");
        $("#input-factura-destino-monto-solicitado-servicio-descuento").val("");
        $("#input-factura-destino-monto-disponible-servicio-descuento").val("");
        //SPAN LABELS
        $(".span-help-block-monto-destino").text("");
        $(".span-help-block-destino-monto-disponible").text("");
        $(".span-help-block-destino-monto-ocupado").text("");
        $(".span-help-block-origen").text("");
        //DIVS MENSAJES COLOR VERDE O ROJO
        $("#div-custom-origen").removeClass("has-success");
        $("#div-custom-origen").removeClass("has-error");
        $("#div-custom-destino").removeClass("has-success");
        $("#div-custom-destino").removeClass("has-error");
        //RESET TABLAS
        $("#tabla-factura-origen-servicio-descuento").hide();
        $("#tabla-nota-credito-solicitada-servicio-descuento").hide();
        $("#tbody-factura-origen-servicio-descuento").html("");
        $("#tbody-nota-credito-solicitada-servicio-descuento").html("");
        //RESET SPANS
        $("#iva-servicio-descuento").text("");
        $("#subtotal-servicio-descuento").text("");
        $("#total-servicio-descuento").text("");
        //RESETEA SELECTS FORMULARIO
        reseteaSelectsFormulario();
    });

    //LIMPIA TODO OTRO DESCUENTO
    document.querySelector("#boton-cancelar-nota-credito-servicio-otro-descuento").addEventListener("click", function (e) {
        //INPUTS FORMULARIO
        //$("#input-cliente").val("");
        $("#boton-solicitar-nota-credito-otro-servicio-descuento").hide();
        $("#input-factura-destino").val("");
        $("#input-factura-origen").val("");
        $("#comentario").val("");
        $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-monto-solicitado-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-cuenta-servicio-otro-descuento-hidden").val("");

        //SPAN LABELS
        $(".span-help-block-monto-destino").text("");
        $(".span-help-block-destino-monto-disponible").text("");
        $(".span-help-block-destino-monto-ocupado").text("");
        $(".span-help-block-origen").text("");

        //DIVS MENSAJES COLOR VERDE O ROJO
        $("#div-custom-origen").removeClass("has-success");
        $("#div-custom-origen").removeClass("has-error");
        $("#div-custom-destino").removeClass("has-success");
        $("#div-custom-destino").removeClass("has-error");

        //ELEMENTOS OTROS DESCUENTOS
        $("#form-otro-concepto-descuento").hide();

        //RESETEA SELECTS FORMULARIO
        reseteaSelectsFormulario();
    });

    function limpiaTodito() {

        //$("#input-cliente").val("");
        $("#input-factura-origen").val("");
        $("#input-factura-destino").val("");
        $("#comentario").val("");
        $("#input-factura-destino-monto-permitido").val("");
        $("#input-factura-destino-monto-solicitado").val("");
        $("#input-factura-destino-monto-disponible").val("");
        //SPAN LABELS
        $(".span-help-block-monto-destino").text("");
        $(".span-help-block-destino-monto-disponible").text("");
        $(".span-help-block-destino-monto-ocupado").text("");
        $(".span-help-block-origen").text("");
        //RESET TABLAS
        $("#tabla-factura-origen-inventario").hide();
        $("#tabla-nota-credito-solicitada-inventario").hide();
        $("#tbody-factura-origen-inventario").html("");
        $("#tbody-nota-credito-solicitada-inventario").html("");
        //RESET SPAN
        $("#iva-inventario").text("");
        $("#subtotal-inventario").text("");
        $("#total-inventario").text("");
        /************************************************************************************/
        $("#input-factura-destino-monto-permitido-servicio-descuento").val("");
        $("#input-factura-destino-monto-solicitado-servicio-descuento").val("");
        $("#input-factura-destino-monto-disponible-servicio-descuento").val("");
        //RESET TABLAS
        $("#tabla-factura-origen-servicio-descuento").hide();
        $("#tabla-nota-credito-solicitada-servicio-descuento").hide();
        $("#tbody-factura-origen-servicio-descuento").html("");
        $("#tbody-nota-credito-solicitada-servicio-descuento").html("");
        //RESET SPANS
        $("#iva-servicio-descuento").text("");
        $("#subtotal-servicio-descuento").text("");
        $("#total-servicio-descuento").text("");
        /************************************************************************************/
        $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-monto-solicitado-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-cuenta-servicio-otro-descuento-hidden").val("");
        //DIVS MENSAJES COLOR VERDE O ROJO
        $("#div-custom-origen").removeClass("has-success");
        $("#div-custom-origen").removeClass("has-error");
        $("#div-custom-destino").removeClass("has-success");
        $("#div-custom-destino").removeClass("has-error");
        //ELEMENTOS OTROS DESCUENTOS
        $("#form-otro-concepto-descuento").hide();
        //RESETEA SELECTS FORMULARIO
        reseteaSelectsFormulario();
    }

    function limpiaToditoMenosCliente() {
        $("#input-factura-origen").val("");
        $("#input-factura-destino").val("");
        $("#comentario").val("");
        $("#input-factura-destino-monto-permitido").val("");
        $("#input-factura-destino-monto-solicitado").val("");
        $("#input-factura-destino-monto-disponible").val("");
        //SPAN LABELS
        $(".span-help-block-monto-destino").text("");
        $(".span-help-block-destino-monto-disponible").text("");
        $(".span-help-block-destino-monto-ocupado").text("");
        $(".span-help-block-origen").text("");
        //RESET TABLAS
        $("#tabla-factura-origen-inventario").hide();
        $("#tabla-nota-credito-solicitada-inventario").hide();
        $("#tbody-factura-origen-inventario").html("");
        $("#tbody-nota-credito-solicitada-inventario").html("");
        //RESET SPAN
        $("#iva-inventario").text("");
        $("#subtotal-inventario").text("");
        $("#total-inventario").text("");
        /************************************************************************************/
        $("#input-factura-destino-monto-permitido-servicio-descuento").val("");
        $("#input-factura-destino-monto-solicitado-servicio-descuento").val("");
        $("#input-factura-destino-monto-disponible-servicio-descuento").val("");
        //RESET TABLAS
        $("#tabla-factura-origen-servicio-descuento").hide();
        $("#tabla-nota-credito-solicitada-servicio-descuento").hide();
        $("#tbody-factura-origen-servicio-descuento").html("");
        $("#tbody-nota-credito-solicitada-servicio-descuento").html("");
        //RESET SPANS
        $("#iva-servicio-descuento").text("");
        $("#subtotal-servicio-descuento").text("");
        $("#total-servicio-descuento").text("");
        /************************************************************************************/
        $("#input-factura-destino-monto-permitido-porcentaje-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-monto-permitido-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-monto-solicitado-servicio-otro-descuento-hidden").val("");
        $("#input-factura-destino-cuenta-servicio-otro-descuento-hidden").val("");
        //DIVS MENSAJES COLOR VERDE O ROJO
        $("#div-custom-origen").removeClass("has-success");
        $("#div-custom-origen").removeClass("has-error");
        $("#div-custom-destino").removeClass("has-success");
        $("#div-custom-destino").removeClass("has-error");
        //ELEMENTOS OTROS DESCUENTOS
        $("#form-otro-concepto-descuento").hide();
        //RESETEA SELECTS FORMULARIO
        reseteaSelectsFormulario();
    }

    $("#input-factura-destino-monto-solicitado-servicio-otro-descuento").click(function () {
        $("#boton-solicitar-nota-credito-otro-servicio-descuento").show();
    });

    //FORMATO DINERO
    var formatNumber = {
        separador: ",",
        sepDecimal: '.',
        formatear: function (num) {
            num += '';
            var splitStr = num.split('.');
            var splitLeft = splitStr[0];
            var splitRight = splitStr.length > 1 ? this.sepDecimal + splitStr[1] : '';
            var regx = /(\d+)(\d{3})/;
            while (regx.test(splitLeft)) {
                splitLeft = splitLeft.replace(regx, '$1' + this.separador + '$2');
            }
            return this.simbol + splitLeft + splitRight;
        },
        new: function (num, simbol) {
            this.simbol = simbol || '';
            return this.formatear(num);
        }
    }

    //ACTUALIZA A SAP
    document.querySelector("#btn-sap").addEventListener("click", function (e) {
        e.preventDefault();
        swal({
            title: "¿Esta seguro de afectar a SAP?",
            text: "Esta operación mandara las notas de credito a SAP",
            icon: "warning",
            buttons: true,
            dangerMode: true,
        })
        .then((willDelete) => {
            if (willDelete) {
                $('#loading-sap').show();
                $.ajax({
                    contentType: "application/json; charset=utf-8",
                    url: '@Url.Action("AffectCreditNoteSap", "Venta")',
                    async: false,
                    timeout: 3000,
                    type: 'POST',
                    data: '{}',
                    dataType: "json",
                }).done(function (data) {

                }).always(function (data) {
                    console.log("always executed in api AffectCreditNoteSap");
                    $('#loading-sap').hide();
                    location.reload();
                }).fail(function (data) {
                    swal("Error en la api: AffectCreditNoteSap");
                });

            } else {
                swal("Operación cancelada");
            }
        });
    });

</script>

<script>
    // notas credito all Administrador

    let rol = localStorage.getItem("Rol");


    if (rol == 'Administrador') {
        GetNotaCreditoAdmin();
    }
    else {
        getHistoricoNotaCredito();
    }

    function GetNotaCreditoAdmin() {
        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetNotaCreditoAdmin", "NotaCredito")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: null,
            dataType: "json",
            success: function (data) {

                if (data != null && $.isArray(data.Context)) {
                    var VP = $('#factura-origen').DataTable();
                    VP.destroy();
                    $("#table-invoice-history").html('');
                    count = data.Context.length;

                    $.each(data.Context, function (index, value) {

                        let EstadoAprobacion = '';
                        if (value.TipoDocumento == 'S') {
                            if (value.Valor == 1 && value.Canal == 'RETAIL') {

                                EstadoAprobacion = 'Aprobación pendiente por credito y cobranza';
                            }
                            else {
                                EstadoAprobacion = 'Aprobación pendiente por dirección';
                            }
                            value.TipoDocumento = 'Servicio';

                        }

                        if (value.TipoDocumento == 'I') {
                            value.TipoDocumento = 'Inventario';
                            if (value.Valor == 1) {
                                EstadoAprobacion = 'Aprobación pendiente por el almacén'
                            }
                        }
                        if (value.Valor == 0) {
                            if (value.ConceptoDescuento == 39) {

                                EstadoAprobacion = 'Aprobación pendiente por credito y cobranza';
                            }
                            else {
                                EstadoAprobacion = 'Aprobación pendiente por gerencia';
                            }
                        }

                        if (value.Valor == 2) {
                            EstadoAprobacion = 'Aprobación pendiente por la dirección'
                        }
                        if (value.Valor == 3) {
                            EstadoAprobacion = 'Autorizada'
                        }
                        if (value.Valor == 4) {
                            EstadoAprobacion = 'SAP'
                        }
                        if (value.Valor == 5) {
                            EstadoAprobacion = 'Rechazada';
                        }
                        if (value.Valor == 10) {
                            EstadoAprobacion = 'No especificado';
                        }
                        $("#table-invoice-history").append(
                            "<tr>\
                                <td>" + value.Identifier + "</td>\
                                <td>" + value.ClienteName + "</td>\
                                <td>" + value.FolioOrigen + "</td>\
                                <td>" + value.FolioDestino + "</td>\
                                <td>" + value.TipoDocumento + "</td>\
                                <td>" + value.ConceptoDescuentoDetalle + "</td>\
                                <td>" + value.Comentario + "</td>\
                                <td>" + value.Usuario + "</td>\
                                <td>" + EstadoAprobacion + "</td>\
                                <td>" + moment(value.Fecha).format("YYYY-MM-DD HH:mm:ss") + "</td>\
                                <td>" + value.FolioSap + "</td>\
                                <td>" + value.FolioPagoSAP + "</td>\
                                <td align='center'>\
                                    <a class='btn btn-primary' data-target='#modal-nota-credito-inventario' data-toggle='modal' onclick='getHistoricoNotaCreditoDetail("+ value.Identifier + ")' href= '#'>\
                                        <i class='fa fa-file-text'></i>\
                                    </a>\
                                </td>\
                                <td align='center'>\
                                    <a class='btn btn-info' data-target='#verComentarios' data-toggle='modal' onclick='verComentarios("+ value.Identifier + ")' data-id='" + value.Identifier + "' href= '#'>\
                                        <i class='fa fa-comment'></i>\
                                    </a>\
                                </td>\
                                <td align='center'>\
                                    <a class='btn btn-info' data-target='#verImagenes' data-toggle='modal' onclick='verImagenesNC("+ value.Identifier + ")' data-id='" + value.Identifier + "' href= '#'>\
                                        <i class='fa fa-eye'></i>\
                                    </a>\
                                </td>\
                                </tr>");
                    });

                    $('#factura-origen').DataTable({
                        "lengthMenu": [[10, 20, 30, -1], [10, 20, 30, "All"]],
                        dom: 'Bfrtip',
                        stateSave: true,
                        buttons: [
                            'excel'
                        ],
                        order: [[1, 'desc']],
                        "paging": true,
                        "searching": true,
                        "language": {
                            "url": "//cdn.datatables.net/plug-ins/1.10.13/i18n/Spanish.json"
                        }
                    });
                }
                if (count > 0) {
                    swal("Verificar notas de credito aplicadas en el historial a la factura actual: " + FOrigen + "");
                }

            },
            error: function () {
                swal("Error en la api: GetNotaCreditoInventario");
            }
        });
    }

    function GetNotaCreditoInventario() {
        // folio de la factura origen
        let FOrigen = $('#input-factura-origen').val();
        let count = 0;

        $.ajax({
            contentType: "application/json; charset=utf-8",
            url: '@Url.Action("GetNotaCreditoInventario", "NotaCredito")',
            async: false,
            timeout: 3000,
            type: 'POST',
            data: '{"FacturaOrigen":"' + FOrigen + '","TipoDocumento":"I"}',
            dataType: "json",
            success: function (data) {

                if (data != null && $.isArray(data.Context)) {

                    // "#table-invoice-history"
                    var VP = $('#factura-origen').DataTable();
                    VP.destroy();
                    $("#table-invoice-history").html('');

                    count = data.Context.length;

                    //console.log("Notas de credito encontradas " + count);

                    $.each(data.Context, function (index, value) {
                        let EstadoAprobacion = '';

                        if (value.TipoDocumento == 'S') {
                            if (value.Valor == 1 && value.Canal == 'RETAIL') {

                                EstadoAprobacion = 'Aprobación pendiente por credito y cobranza';
                            }
                            else {
                                EstadoAprobacion = 'Aprobación pendiente por dirección';
                            }
                            value.TipoDocumento = 'Servicio';
                        }

                        if (value.TipoDocumento == 'I') {
                            value.TipoDocumento = 'Inventario';
                            if (value.Estatus == 1) {
                                value.Estatus = 'Aprobación pendiente por el almacén'
                            }
                        }

                        if (value.Valor == 0) {
                            if (value.ConceptoDescuento == 39) {

                                EstadoAprobacion = 'Aprobación pendiente por credito y cobranza';
                            }
                            else {
                                EstadoAprobacion = 'Aprobación pendiente por gerencia';
                            }
                        }

                        if (value.Valor == 2) {
                            EstadoAprobacion = 'Aprobación pendiente por la dirección'
                        }
                        if (value.Valor == 3) {
                            EstadoAprobacion = 'Autorizada'
                        }
                        if (value.Valor == 4) {
                            EstadoAprobacion = 'SAP'
                        }
                        if (value.Valor == 5) {
                            EstadoAprobacion = 'Rechazada';
                        }
                        if (value.Valor == 10) {
                            EstadoAprobacion = 'No especificado';
                        }
                        $("#table-invoice-history").append(
                            "<tr>\
                                <td>" + value.Identifier + "</td>\
                                <td>" + value.ClienteName + "</td>\
                                <td>" + value.FolioOrigen + "</td>\
                                <td>" + value.FolioDestino + "</td>\
                                <td>" + value.TipoDocumento + "</td>\
                                <td>" + value.ConceptoDescuentoDetalle + "</td>\
                                <td>" + value.Comentario + "</td>\
                                <td>" + value.Usuario + "</td>\
                                <td>" + EstadoAprobacion + "</td>\
                                <td>" + moment(value.Fecha).format("YYYY-MM-DD HH:mm:ss") + "</td>\
                                <td>" + value.FolioSap + "</td>\
                                <td>" + value.FolioPagoSAP + "</td>\
                                <td align='center'>\
                                    <a class='btn btn-primary' data-target='#modal-nota-credito-inventario' data-toggle='modal' onclick='getHistoricoNotaCreditoDetail("+ value.Identifier + ")' href= '#'>\
                                        <i class='fa fa-file-text'></i>\
                                    </a>\
                                </td>\
                                <td align='center'>\
                                    <a class='btn btn-info' data-target='#verComentarios' data-toggle='modal' onclick='verComentarios("+ value.Identifier + ")' data-id='" + value.Identifier + "' href= '#'>\
                                        <i class='fa fa-comment'></i>\
                                    </a>\
                                </td>\
                                <td align='center'>\
                                    <a class='btn btn-info' data-target='#verImagenes' data-toggle='modal' onclick='verImagenesNC("+ value.Identifier + ")' data-id='" + value.Identifier + "' href= '#'>\
                                        <i class='fa fa-eye'></i>\
                                    </a>\
                                </td>\
                                </tr>");
                    });

                    $('#factura-origen').DataTable({
                        "lengthMenu": [[10, 20, 30, -1], [10, 20, 30, "All"]],
                        dom: 'Bfrtip',
                        stateSave: true,
                        buttons: [
                            'excel'
                        ],
                        order: [[1, 'desc']],
                        "paging": true,
                        "searching": true,
                        "language": {
                            "url": "//cdn.datatables.net/plug-ins/1.10.13/i18n/Spanish.json"
                        }
                    });
                }
                if (count > 0) {
                    swal("Verificar notas de credito aplicadas en el historial a la factura actual: " + FOrigen + "");
                }

            },
            error: function () {
                swal("Error en la api: GetNotaCreditoInventario");
            }
        });
    }
</script>